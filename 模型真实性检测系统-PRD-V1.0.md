# 模型真实性检测系统 PRD

版本：V1.0  
状态：V1.0 冻结版  
日期：2026-09-04  
冻结日期：2026-09-04  
冻结依据：`docs/decisions/M0-01-V1.0-决策记录.md`  
产品代号：Model Integrity Inspector（MII）  
项目形态：独立部署、独立账号体系、独立数据存储  
适用范围：通过 API Key 调用的 OpenAI-compatible 文本模型接口

---

## 1. 文档目的

本文定义“模型真实性检测系统”的产品目标、使用者、业务流程、功能范围、检测口径、页面要求、权限、安全要求、验收标准和版本计划，作为产品、研发、测试、运维及交付共同使用的需求基线。

与本文配套的技术实现见《模型真实性检测系统 TECH SPEC V1.0》。若两份文档存在冲突：产品行为和验收口径以 PRD 为准，内部实现以 TECH SPEC 为准。

---

## 2. 背景与问题

模型聚合平台、代理网关或第三方模型服务可能在不透明的情况下改变请求或响应。例如：

- 在用户提示词之外添加隐藏 system/developer 指令；
- 修改或限制 `max_tokens`、`max_completion_tokens`；
- 在流式输出中提前断流或二次截断；
- 改写请求参数、响应正文、`finish_reason` 或 Usage；
- 宣称提供某模型，但实际行为与可信基线存在显著偏差。

这些问题通常无法依靠一次普通问答识别。调用方需要一套可重复、可对照、可审计的检测工具，使用参数化探针、多轮采样和统计分析发现异常，并保留能够复现的证据。

黑盒检测不能读取供应商内部配置，因此系统只能证明“观测到的行为异常”，不能仅凭模型输出证明某段隐藏提示词的准确原文，也不能在没有服务端证据时直接认定供应商存在欺诈。

---

## 3. 产品定位

### 3.1 一句话定位

面向模型渠道运营与技术审计人员的 API 模型完整性检测工具，通过受控测试判断接口是否存在隐藏指令影响、输出 Token 上限异常和响应链路篡改风险。

### 3.2 产品形态

产品以独立 Web 系统整体交付，包含管理端、后端 API、检测 Worker、分析引擎、数据库迁移、账号权限、报告和部署脚本。用户在系统内维护供应商、Endpoint、API Key 和模型档案后发起检测。

项目不依赖 New API 或其他渠道管理、账号、权限、任务、报告系统，也不读取其他业务项目的数据库。未来若需与外部系统连接，只能通过公开、可选的 API/Webhook 集成，不得成为核心功能运行的前置条件。

支持两种由本项目自行提供的安装模式：

1. **单机一体化**：一个应用进程内运行 Web/API/Worker，使用项目自带的静态前端和本地数据目录，适合试用与小规模部署。
2. **标准生产部署**：API 与 Worker 可水平扩展，配套数据库由本项目 Docker Compose/Helm 安装清单统一编排。

### 3.3 价值主张

- 用批量受控实验替代人工“凭感觉”判断；
- 将异常结论关联到原始请求、响应和统计数据；
- 对不同渠道、模型、时间批次进行横向与纵向比较；
- 在不暴露 API Key 和用户业务内容的前提下形成审计报告；
- 为渠道准入、复检、降级和人工复核提供依据。

---

## 4. 产品目标与非目标

### 4.1 MVP 目标

1. 支持 OpenAI-compatible Chat Completions 文本接口检测。
2. 识别稳定的隐藏提示词行为影响并给出风险等级。
3. 识别固定或分段式 `max_tokens` 隐性上限、提前截断和参数不生效。
4. 保存脱敏后的请求、响应、Usage、响应头摘要和时间数据。
5. 输出检测项得分、证据等级、置信度、局限性和复现方式。
6. 支持同一目标重复检测和历史趋势对比。
7. 支持可控阳性代理环境，用于验证检测器自身准确率。
8. 自主管理供应商、检测目标、模型档案、账号、权限和审计数据。
9. 提供一键初始化、升级迁移、备份恢复和健康检查能力，满足独立上线要求。

### 4.2 成功指标

在官方验收数据集上：

| 指标 | MVP 目标 |
|---|---:|
| 固定输出上限篡改检出率 | ≥ 95% |
| 分段式输出上限篡改检出率 | ≥ 90% |
| 稳定隐藏提示词行为影响检出率 | ≥ 85% |
| 正常接口误报率 | ≤ 5% |
| 可复现证据覆盖率 | 100% |
| 检测任务完成率（排除上游不可用） | ≥ 99% |
| API Key 明文进入日志/报告次数 | 0 |
| 默认标准检测包单目标调用量 | ≤ 60 次 |
| 全新环境 Docker Compose 初始化时间 | ≤ 15 分钟（镜像下载时间除外） |
| 备份恢复演练成功率 | 100% |

准确率以冻结的验收集和规则版本计算，不以研发调参所使用的数据集计算。

### 4.3 非目标

MVP 不承诺：

- 还原隐藏 system/developer 提示词的准确原文；
- 仅凭行为百分之百确认服务商主观欺诈；
- 精确识别所有模型型号、权重版本或量化方式；
- 绕过模型安全策略或探测受保护的系统提示词；
- 支持图像、语音、视频等多模态真实性检测；
- 替代供应商合同、账单或服务端审计；
- 自动封禁生产渠道；
- 在无可信 tokenizer 时给出绝对精确的本地 Token 数。

---

## 5. 核心术语与统一口径

| 术语 | 定义 |
|---|---|
| 检测目标 Target | Endpoint、鉴权配置、协议、模型名及可选渠道关联组成的待测对象 |
| 检测任务 Run | 对一个目标、一个策略版本执行的一次完整测试 |
| 探针 Probe | 一种检测目的明确、可参数化生成多个样本的测试方法 |
| 样本 Sample | 一次实际 API 请求及其响应、测量数据和判定结果 |
| 对照组 Baseline | 官方接口、可信接口或目标自身历史稳定批次形成的参考数据 |
| 隐藏指令影响 | 用户未提供的高优先级指令对输出格式、内容、拒答或风格造成稳定影响 |
| Token 上限异常 | 实际可生成长度与请求参数、协议语义或可信基线显著不一致 |
| 硬截断 | 文本在结构、句子或编码单位未完成时停止，常伴随异常流结束或长度聚集 |
| 风险指数 | 根据可观测异常计算的 0～100 数值，越高表示风险越大 |
| 置信度 | 当前样本量、重复性、效应量和证据质量对结论可靠程度的估计 |
| 模型完整性风险指数 | 产品展示名称，等同于综合风险指数；不表示已证明供应商欺诈或篡改 |
| 证据等级 | 对结论证据强弱的 A/B/C/D 分级 |

### 5.1 证据等级

| 等级 | 定义 | 示例 |
|---|---|---|
| A：直接证据 | 可观测到请求在受控网关中被修改，或供应商返回明确参数回显 | 网关审计日志显示 `max_tokens` 从 1024 改为 256 |
| B：强统计证据 | 多探针、多轮重复出现显著且稳定的异常，可排除常见自然停止 | 5 个长度档位在 20 次请求中均集中于同一硬上限 |
| C：行为迹象 | 存在异常，但可能由安全策略、模型随机性或协议差异解释 | 固定格式任务偶发出现未要求的前缀 |
| D：信息不足 | 请求失败、样本不足、模型不适用或 tokenizer 不可用 | 上游持续限流导致有效样本不足 |

任何黑盒检测结果默认最高为 B；只有获得服务端或受控代理的直接证据时才能标记 A。

---

## 6. 用户与角色

### 6.1 目标用户

| 角色 | 主要诉求 |
|---|---|
| 平台管理员 | 配置检测、管理密钥、查看全部渠道结果、制定准入规则 |
| 渠道运营 | 比较渠道质量，执行准入检测和周期复检 |
| 安全审计员 | 查看证据链、规则版本、原始数据摘要和审计日志 |
| 开发/测试人员 | 调试协议兼容性，验证代理层参数是否被改写 |
| 只读观察者 | 查看允许范围内的报告，不接触密钥和敏感响应 |

### 6.2 权限矩阵

| 能力 | 管理员 | 运营 | 审计员 | 开发测试 | 只读 |
|---|:---:|:---:|:---:|:---:|:---:|
| 创建/编辑目标 | ✓ | ✓ | — | ✓ | — |
| 查看 API Key 明文 | — | — | — | — | — |
| 更新/替换 API Key | ✓ | 按授权 | — | 按授权 | — |
| 发起标准检测 | ✓ | ✓ | ✓ | ✓ | — |
| 发起高成本检测 | ✓ | 按授权 | ✓ | 按授权 | — |
| 停止任务 | ✓ | 自己创建 | ✓ | 自己创建 | — |
| 查看脱敏证据 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 查看受限响应正文 | ✓ | 按授权 | ✓ | 按授权 | — |
| 修改规则/阈值 | ✓ | — | — | — | — |
| 导出报告 | ✓ | ✓ | ✓ | ✓ | 按授权 |
| 删除任务数据 | ✓ | — | — | — | — |

API Key 采用“只可替换，不可回显”的原则；任何角色均不能从界面取回完整密钥。

---

## 7. 核心使用场景

### 7.1 新渠道准入

运营新增 Endpoint、Key 和模型名，先执行连接检查，再执行标准检测包。系统形成报告，运营根据风险和证据决定通过、人工复核或拒绝准入。

### 7.2 已上线渠道周期复检

管理员对生产渠道设置日/周复检策略。系统比较本次与历史稳定基线，异常时通知运营，但 MVP 不自动禁用渠道。

### 7.3 多渠道横向比较

用户选择相同标准模型下的多个渠道，以相同随机化探针和近似参数执行测试，比较隐藏指令风险、Token 参数一致性、失败率、延迟和成本。

### 7.4 参数覆盖问题排查

开发人员怀疑网关把 `max_tokens=1024` 改成 256。系统执行多档位长度测试，展示请求值、返回 Usage、本地估算、停止原因和输出长度分布。

### 7.5 提示词注入问题排查

审计员发现模型无故添加固定免责声明或改变回答身份。系统执行格式契约、成对差分、多语言和中性拒答探针，判断行为是否稳定且与基线显著不同。

### 7.6 受控代理验收

测试人员在实验环境分别开启隐藏 system 提示、Token 上限覆盖、Usage 伪造和流式截断开关，运行验收集，验证检测规则的检出率与误报率。

---

## 8. 业务流程

### 8.1 标准检测流程

```text
创建检测目标
  → 连通性与协议预检
  → 选择检测包和预算
  → 生成本次随机化探针
  → 执行样本并实时展示进度
  → 样本级规则判定
  → 探针级统计聚合
  → 综合风险与置信度计算
  → 人工复核（可选）
  → 报告冻结与导出
```

### 8.2 任务状态

| 状态 | 含义 |
|---|---|
| DRAFT | 已创建但未执行 |
| PRECHECKING | 正在验证鉴权、模型和协议 |
| QUEUED | 已进入执行队列 |
| RUNNING | 正在调用上游 |
| ANALYZING | 正在进行统计和评分 |
| REVIEW_REQUIRED | 数据异常或置信度不足，需要人工复核 |
| COMPLETED | 检测完成并生成报告 |
| PARTIAL | 部分探针完成，结论受限 |
| FAILED | 无法形成有效结论 |
| CANCELLED | 用户主动停止 |

### 8.3 任务停止条件

- 达到用户配置的最大请求次数；
- 达到最大预算；
- 达到最长执行时间；
- 连续鉴权失败；
- 连续不可重试错误超过阈值；
- 用户取消；
- 熔断策略触发；
- 已获得足够统计证据且启用“提前结束”。

---

## 9. 功能需求

需求优先级使用 P0（MVP 必须）、P1（首个增强版本）、P2（后续）。

### 9.0 独立系统基础能力

| 编号 | 需求 | 优先级 |
|---|---|---|
| SYS-001 | 首次启动提供初始化流程，创建系统配置、首个组织和初始管理员 | P0 |
| SYS-002 | 自带用户名/密码登录、退出、会话管理和密码修改能力 | P0 |
| SYS-003 | 自带用户、组织、成员、角色和权限管理 | P0 |
| SYS-004 | 自带供应商与模型档案管理，不依赖外部模型目录 | P0 |
| SYS-005 | 自带任务队列、Worker、失败恢复和任务清理能力 | P0 |
| SYS-006 | 自带数据库迁移、版本检查、健康检查和运行状态页 | P0 |
| SYS-007 | 提供项目自身的 Docker 镜像及 Docker Compose 部署清单 | P0 |
| SYS-008 | 支持从系统内导出配置与数据备份，并提供恢复校验 | P0 |
| SYS-009 | 可选接入 OIDC/SSO，不得作为本地部署的强制依赖 | P1 |
| SYS-010 | 可选通过通用 REST API/Webhook 与外部平台集成 | P1 |

### 9.1 检测目标管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| TAR-001 | 创建目标：名称、Endpoint、协议、API Key、模型名 | P0 |
| TAR-002 | 配置组织、环境、渠道 ID、标准模型和标签 | P0 |
| TAR-003 | API Key 加密保存、掩码显示、只可替换 | P0 |
| TAR-004 | 执行连通性、鉴权、模型存在性和流式兼容检查 | P0 |
| TAR-005 | 支持自定义请求头，但禁止覆盖系统保留头 | P0 |
| TAR-006 | 支持代理、超时、区域和 TLS 校验设置 | P1 |
| TAR-007 | 一个目标绑定多个模型配置 | P1 |
| TAR-008 | 在系统内维护供应商与模型档案，并与检测目标关联 | P0 |

### 9.2 检测策略与执行

| 编号 | 需求 | 优先级 |
|---|---|---|
| RUN-001 | 提供快速、标准、深度、自定义四种检测包 | P0 |
| RUN-002 | 运行前显示预计请求量、Token 量、费用和时间 | P0 |
| RUN-003 | 支持并发数、重复次数、超时、重试和预算上限 | P0 |
| RUN-004 | 每次运行冻结规则版本、提示模板版本和参数 | P0 |
| RUN-005 | 同批次使用随机 nonce 和动态样本，避免固定题库 | P0 |
| RUN-006 | 实时展示总进度、各探针进度、成功/失败数和消耗 | P0 |
| RUN-007 | 支持安全取消；已完成样本保留并标记取消 | P0 |
| RUN-008 | 允许对失败或可疑探针单独复测；复测形成新任务或修订，不覆盖原始样本 | P0 |
| RUN-009 | 支持定时复检和异常通知 | P1 |
| RUN-010 | 支持多目标同策略对照运行 | P1 |

建议默认检测包：

| 检测包 | 请求量目标 | 用途 |
|---|---:|---|
| 快速 | 12～20 | 接入检查和初筛，不输出高置信度无风险结论 |
| 标准 | 36～60 | MVP 默认，支持正式内部报告 |
| 深度 | 80～150 | 高价值渠道、争议复核和基线建设 |
| 自定义 | 用户配置 | 研发与审计实验 |

### 9.3 提示词注入行为检测

系统不尝试诱导模型泄露系统提示词，而是检测隐藏指令是否对可观测行为造成稳定影响。

| 编号 | 探针 | 产品要求 | 优先级 |
|---|---|---|---|
| PINJ-001 | 格式契约探针 | 要求只输出随机结构或短标记，检测固定前后缀、额外解释和格式破坏 | P0 |
| PINJ-002 | 成对差分探针 | 仅改变一个无关变量，判断输出是否受到非用户指令稳定影响 | P0 |
| PINJ-003 | 中性拒答探针 | 对无风险任务测试异常拒答、身份声明和固定免责声明 | P0 |
| PINJ-004 | 身份/风格偏置探针 | 检测持续自称特定身份、固定语气或强制话题偏移 | P0 |
| PINJ-005 | 多语言等价探针 | 对语义等价的中英文请求比较行为差异 | P0 |
| PINJ-006 | 顺序与位置探针 | 改变用户内容顺序，判断隐藏前后缀影响 | P1 |
| PINJ-007 | 工具调用契约探针 | 检测隐藏指令对函数调用选择和参数的影响 | P2 |
| PINJ-008 | 自报告问题 | 仅作为辅助信息，禁止单独参与确定性结论 | P0 |

探针输出必须包括：异常类型、样本数、重复率、效应量、与基线差异、可能的替代解释和证据等级。

### 9.4 `max_tokens` 与输出截断检测

| 编号 | 探针 | 产品要求 | 优先级 |
|---|---|---|---|
| MTOK-001 | 多档位阶梯测试 | 至少测试 64/128/256/512 四档，可按模型调整 | P0 |
| MTOK-002 | 持续生成任务 | 使用不易自然提前结束且可验证完整性的任务 | P0 |
| MTOK-003 | 结构完整性检测 | 判断 JSON、编号序列、句子或代码块是否未完成 | P0 |
| MTOK-004 | 停止原因一致性 | 比较 `finish_reason`、协议事件、输出形态和 Token 数 | P0 |
| MTOK-005 | Usage 交叉校验 | 比较上游 Usage 与本地 tokenizer/估算器 | P0 |
| MTOK-006 | 流式/非流式对照 | 检测仅在某种传输模式发生的提前截断 | P0 |
| MTOK-007 | 参数别名兼容 | 识别 `max_tokens` 与 `max_completion_tokens` 的协议差异 | P0 |
| MTOK-008 | 上下文窗口测试 | 检测输入+输出总长度能力 | P1 |
| MTOK-009 | 隐性分段上限 | 识别按账户、模型、时间或请求类型变化的限制 | P1 |

系统不得因为单次输出短于请求值就判定篡改。必须综合多档位、重复采样、结构完整性和停止原因。

### 9.5 响应完整性检测

| 编号 | 需求 | 优先级 |
|---|---|---|
| RINT-001 | 检测固定前缀、后缀、免责声明或代理水印 | P0 |
| RINT-002 | 检测 SSE 提前结束、缺少结束事件和异常断连 | P0 |
| RINT-003 | 检测正文与 Usage、finish_reason 的明显矛盾 | P0 |
| RINT-004 | 比较流式与非流式内容长度和结束方式 | P0 |
| RINT-005 | 记录响应头白名单字段及请求追踪 ID | P0 |
| RINT-006 | 检测响应 JSON 字段被删除、添加或类型改变 | P1 |

### 9.6 基线管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| BASE-001 | 支持将已审核任务设为可信基线 | P0 |
| BASE-002 | 基线绑定模型、协议、参数、区域、日期和规则版本 | P0 |
| BASE-003 | 基线过期后提示重新采样 | P0 |
| BASE-004 | 支持官方接口基线和目标自身历史基线 | P0 |
| BASE-005 | 基线不能被普通用户静默覆盖 | P0 |
| BASE-006 | 支持同批随机种子/探针变量的成对比较 | P1 |

### 9.7 评分、结论与人工复核

系统输出四个一级指标：

1. 隐藏指令影响风险；
2. Token 参数一致性风险；
3. 响应完整性风险；
4. 协议与证据可信度风险。

综合风险指数默认权重：

```text
综合风险指数 =
  40% × 隐藏指令影响风险
+ 35% × Token 参数一致性风险
+ 15% × 响应完整性风险
+ 10% × 协议与证据可信度风险
```

权重必须版本化且支持管理员配置。若关键检测项无有效数据，系统不得简单将其按 0 分计入，应重新归一化已有指标并显著标注“结论不完整”。

默认风险等级：

| 指数 | 等级 | 展示建议 |
|---:|---|---|
| 0～19 | 低 | 当前样本未发现明显异常 |
| 20～39 | 关注 | 存在轻微或不稳定迹象，建议复测 |
| 40～69 | 中 | 存在可重复异常，需要人工复核 |
| 70～89 | 高 | 多项强异常，不建议直接准入 |
| 90～100 | 严重 | 存在直接证据或高度一致的强统计证据 |

限制：无 A 级直接证据时，“严重”结论必须显示“黑盒统计判断”，不能显示“已确认篡改”。

人工复核能力：

- 标记结论为确认、误报、待观察或不适用；
- 填写复核说明；
- 调整业务处置，但不能修改原始算法分；
- 报告同时展示算法结论和人工结论；
- 所有复核动作进入审计日志。

### 9.8 证据与报告

| 编号 | 需求 | 优先级 |
|---|---|---|
| REP-001 | 任务总览包含风险、置信度、证据等级和完整性声明 | P0 |
| REP-002 | 每项结论可下钻到探针和样本 | P0 |
| REP-003 | 样本展示请求参数、脱敏正文、响应、Usage、停止原因和耗时 | P0 |
| REP-004 | 给出替代解释与检测局限性 | P0 |
| REP-005 | 输出复现用请求模板，但不包含真实 Key | P0 |
| REP-006 | 支持 JSON 和 HTML 导出 | P0 |
| REP-007 | 支持 PDF/CSV 导出 | P1 |
| REP-008 | 报告冻结后生成内容哈希，后续修改形成新版本 | P1 |
| REP-009 | 支持两个任务的差异报告 | P1 |

报告必须包含以下免责声明：

> 本报告基于指定时间、接口、参数和样本的可观测行为生成。除非存在直接网关证据，否则异常代表风险或统计迹象，不等同于对供应商内部配置或主观行为的确定性证明。

### 9.9 历史与趋势

| 编号 | 需求 | 优先级 |
|---|---|---|
| HIS-001 | 按目标展示历次风险、置信度、调用成功率和延迟 | P0 |
| HIS-002 | 检测规则升级时区分“行为变化”和“算法变化” | P0 |
| HIS-003 | 可筛选模型、渠道、日期、风险项和任务状态 | P0 |
| HIS-004 | 支持固定基线下重新分析历史原始数据 | P1 |
| HIS-005 | 显著恶化时产生站内通知/Webhook | P1 |

### 9.10 配置与规则管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| CFG-001 | 检测模板、阈值、评分权重全部版本化 | P0 |
| CFG-002 | 已执行任务始终引用不可变规则快照 | P0 |
| CFG-003 | 规则发布前支持离线回放验收集 | P0 |
| CFG-004 | 规则变更记录操作者、差异和发布时间 | P0 |
| CFG-005 | 支持灰度启用新规则 | P1 |
| CFG-006 | 探针模板中的敏感内容须经过安全评审 | P0 |

---

## 10. 页面与交互要求

### 10.1 页面清单

1. **检测总览**：目标数、近 7/30 天任务数、风险分布、异常趋势。
2. **检测目标列表**：名称、模型、渠道、最近检测、风险、状态和快捷操作。
3. **新建/编辑目标**：Endpoint、Key、协议、模型、Header、连通性测试。
4. **发起检测**：检测包、预算、重复次数、流式模式、基线和高级选项。
5. **任务运行页**：阶段进度、探针进度、调用数、Token、预计费用、错误。
6. **结果总览**：综合风险、四项分数、置信度、证据等级和关键发现。
7. **探针详情**：样本分布、对照差异、阈值、替代解释。
8. **样本证据页**：脱敏请求/响应、Usage、事件时间线和重试链。
9. **任务对比页**：两个或多个任务的指标差异。
10. **基线管理页**：基线来源、适用范围、样本量、到期时间和审核状态。
11. **规则管理页**：规则版本、检测包、阈值、发布和回放结果。
12. **审计日志页**：密钥变更、运行、导出、复核、规则发布和数据删除。

### 10.2 结果页信息层级

首屏必须回答：

- 是否发现异常；
- 哪一类异常最严重；
- 结论可信度如何；
- 是直接证据还是统计迹象；
- 下一步建议是什么。

建议固定展示结构：

```text
综合风险 72（高）    置信度 86%    最高证据 B
关键发现：输出在请求 512/1024 tokens 时均聚集于约 256 tokens
建议：复测并核查代理网关参数覆盖；在确认前暂停该渠道新增流量
限制：本任务未获得服务端请求回显，不能直接证明参数被修改
```

### 10.3 敏感信息展示

- API Key 始终显示为掩码；
- 自定义 Header 中的鉴权类字段自动掩码；
- 请求正文默认只显示系统生成的探针，不显示用户真实业务数据；
- 响应正文按权限显示，并支持管理员一键关闭正文留存；
- 导出前显示将包含的数据范围和敏感级别。

---

## 11. 数据与隐私要求

### 11.1 数据分类

| 等级 | 数据 | 默认策略 |
|---|---|---|
| S3 极敏感 | API Key、代理凭证 | 信封加密；不记录日志；不可导出；不可回显 |
| S2 敏感 | 完整请求/响应正文、Endpoint 私有参数 | 权限控制；可配置不落库；导出需审计 |
| S1 内部 | 评分、Usage、延迟、渠道关系 | 组织隔离；按角色访问 |
| S0 普通 | 公共规则版本、探针类型 | 可普通内部访问 |

### 11.2 默认保留策略

- API Key：目标存在期间加密保存，删除目标后立即删除密文和数据密钥引用；
- 完整响应正文：默认 30 天，可配置 0～180 天；
- 脱敏样本摘要：默认 180 天；
- 聚合结果和报告：默认 365 天；
- 审计日志：默认 365 天且不可由普通用户删除；
- 删除操作采用异步物理删除，并记录不可包含敏感正文的删除凭证。

### 11.3 数据最小化

- 产品探针必须使用合成数据，不发送真实客户内容；
- 默认不采集上游返回的非白名单响应头；
- 日志只记录目标 ID、任务 ID、样本 ID 和错误分类；
- 可观测平台禁止采集请求正文和鉴权头；
- 调试模式有时限、授权和醒目标识，超时自动关闭。

---

## 12. 非功能需求

### 12.1 性能与容量

| 项目 | MVP 指标 |
|---|---:|
| 单实例并发运行任务 | ≥ 20 |
| 单实例并发上游请求 | ≥ 100，可配置 |
| 任务状态查询 P95 | < 500 ms |
| 普通列表查询 P95 | < 800 ms |
| 任务取消生效时间 | < 5 s（不含已发出请求） |
| 分析阶段耗时 | 标准包 < 30 s |
| 报告生成时间 | HTML < 10 s |

系统必须为每个目标和组织设置独立并发限制，避免单个目标耗尽独立系统的执行资源。

### 12.2 可用性

- 控制面月可用性目标 99.9%；
- Worker 异常退出后，已持久化任务可恢复或明确失败；
- 同一样本通过幂等键避免非预期重复计费；
- 上游不可用不能导致队列整体阻塞；
- 报告生成失败不应丢失检测结果。

### 12.3 安全

- 所有外部请求必须防范 SSRF；
- 默认禁止访问回环、链路本地和内网地址，内网部署按白名单开放；
- 校验 DNS 重绑定和重定向后的最终地址；
- TLS 校验默认开启；
- API Key 使用独立主密钥进行信封加密；
- 敏感操作进入不可篡改审计日志；
- 系统自带登录、组织隔离、RBAC、会话、初始管理员和密码安全策略；
- 可选接入 OIDC/SSO，但本地管理员登录始终可作为受控的应急入口；
- 探针模板不得包含真实密钥、个人数据或违法内容；
- 导出的复现命令使用 `${API_KEY}` 占位符。

### 12.4 可解释性

每个风险结论必须能回答：

- 哪条规则触发；
- 使用了哪些样本；
- 阈值和实际值分别是多少；
- 是否有对照组；
- 结论可能受到哪些因素影响；
- 使用的是哪个算法、规则和模板版本。

### 12.5 兼容性

MVP 必须兼容：

- `POST /v1/chat/completions`；
- 非流式 JSON 响应；
- `text/event-stream` 流式响应；
- 常见 `usage` 与 `finish_reason` 变体；
- `max_tokens` 和 `max_completion_tokens` 配置映射；
- 标准 Bearer 鉴权和自定义鉴权 Header。

不兼容的接口必须在预检阶段明确提示，不能以“模型异常”计分。

---

## 13. 通知与处置

### 13.1 通知条件

P1 支持以下通知：

- 综合风险从低/关注升至中及以上；
- 任一一级风险上升超过 25 分；
- 出现 A 级直接证据；
- 连续两次任务失败；
- 基线即将到期；
- 周期任务预算耗尽。

### 13.2 处置建议

系统只能给出处置建议，MVP 不自动改变渠道生产状态：

| 结果 | 建议 |
|---|---|
| 低风险 + 高置信度 | 可继续准入流程，按计划复检 |
| 关注 | 增加样本或使用可信基线复测 |
| 中风险 | 人工复核代理配置和供应商文档 |
| 高风险 | 暂停新增流量，执行深度检测和网关审计 |
| 严重 + A 级证据 | 按组织流程冻结渠道并联系供应商 |
| 信息不足 | 修复鉴权/限流/兼容问题后重新检测 |

若未来增加自动降权或禁用，必须另行设计审批、冷却时间、回滚和人工恢复机制。

---

## 14. 分析与运营指标

系统应采集但不包含敏感正文的产品指标：

- 每日任务数、目标数和成功率；
- 各检测包使用比例；
- 每任务请求量、Token 量、费用和耗时；
- 各探针有效样本率；
- 风险等级分布和变化；
- 人工复核率、确认率和误报率；
- 规则版本间的命中差异；
- 上游错误类型、限流率和超时率；
- 周期复检覆盖率。

算法质量看板必须区分开发集、回归集和冻结验收集。

---

## 15. MVP 验收标准

### 15.1 功能验收

1. 可创建 OpenAI-compatible 检测目标，Key 全链路不以明文展示或记录。
2. 可完成预检并正确区分鉴权、模型不存在、协议不兼容和网络错误。
3. 可执行快速、标准、深度检测包并显示预算预估和实时进度。
4. 至少包含 5 类提示词注入行为探针和 6 类 Token/完整性探针。
5. 同一任务冻结其配置、规则、模板和评分版本。
6. 结果可从综合风险下钻到探针及样本级证据。
7. 任务支持取消、部分完成、失败恢复和探针复测。
8. 支持可信基线、历史趋势、HTML 和 JSON 导出。
9. 报告包含证据等级、置信度、替代解释和免责声明。
10. 具备管理员、运营、审计员、开发测试和只读权限控制。
11. 全新环境不安装任何其他业务项目即可完成初始化、登录、目标创建、检测和报告导出。
12. 单机包和 Docker Compose 均有明确安装步骤、健康检查、数据库迁移和升级检查。
13. 可完成一次备份、清空测试环境、恢复并校验目标、任务、报告与审计数据。

### 15.2 算法验收

验收环境至少包含：

- 未修改的可信接口；
- 添加稳定隐藏 system 指令的代理；
- 添加固定输出前缀/后缀的代理；
- 强制将 512/1024 上限改成 256 的代理；
- 按档位或概率修改上限的代理；
- 修改 Usage 但不修改正文的代理；
- 仅对流式响应提前断流的代理；
- 正常安全策略导致拒答的困难阴性样本；
- 会自然短答或主动 EOS 的困难阴性样本。

验收采用盲测：规则发布者不能在验收时看到场景标签。统计指标达到第 4.2 节目标方可发布。

### 15.3 安全验收

- 日志、错误追踪、数据库普通查询和导出中搜索不到完整 API Key；
- SSRF 测试覆盖回环、私网、云元数据地址、DNS 重绑定和重定向；
- 组织 A 无法读取组织 B 的目标、任务、样本或报告；
- 导出和受限正文访问均产生审计记录；
- 密钥轮换不影响历史报告且旧密钥不可恢复；
- 备份数据中的密钥同样加密。

### 15.4 性能验收

- 达到第 12.1 节指标；
- 100 并发调用时无任务状态错乱或样本重复统计；
- Worker 重启后任务不出现“永久运行中”；
- 队列积压不会导致管理端不可用，也不会阻塞其他组织查询历史结果；
- 大响应正文被限制大小，不造成内存持续增长。

---

## 16. 版本规划

### 16.1 MVP / V1.0

- OpenAI-compatible Chat Completions；
- 独立管理端、后端 API、检测 Worker、分析引擎与账号权限体系；
- 独立供应商、模型档案和检测目标管理；
- 提示词注入行为、Token 上限、流式完整性检测；
- 快速/标准/深度检测包；
- 基线、评分、证据下钻、HTML/JSON 报告；
- RBAC、密钥加密、审计日志；
- 可控阳性代理和自动化验收集。

### 16.2 V1.1

- 周期复检、Webhook 通知；
- 多渠道同批对照；
- PDF/CSV 报告；
- 历史原始数据重放；
- 分段式上限和上下文窗口增强检测；
- Responses API 适配。

### 16.3 V1.2+

- Anthropic/Gemini 原生协议；
- 工具调用和结构化输出检测；
- 模型行为指纹与型号一致性辅助判断；
- 多模态模型检测；
- 经审批的渠道自动降权/隔离；
- 跨组织匿名基线（需隐私与合规评审）。

---

## 17. 建议排期与里程碑

以 1 名产品、2 名后端、1 名前端、1 名测试/算法工程师为参考：

| 周期 | 里程碑 | 交付物 |
|---|---|---|
| 第 1 周 | 需求冻结与实验环境 | PRD/Spec 评审、原型、受控代理、验收集框架 |
| 第 2 周 | 独立系统底座 | 初始化、登录、组织/RBAC、供应商/模型档案、数据库任务队列 |
| 第 3 周 | API 调用链路 | 目标、密钥、协议适配、Worker、样本采集和预算 |
| 第 4 周 | Token 检测 | 阶梯探针、流/非流对照、Usage 校验、初版规则 |
| 第 5 周 | 注入检测 | 格式契约、差分、多语言、拒答与身份探针 |
| 第 6 周 | 评分与报告 | 聚合评分、证据下钻、基线、HTML/JSON 报告 |
| 第 7 周 | 管理端与独立交付 | 完整页面、审计、单机包、Docker Compose、备份恢复 |
| 第 8 周 | 回归与校准 | 盲测、误报分析、性能/安全/恢复测试、阈值冻结 |
| 第 9 周 | 灰度发布 | 小流量试用、监控、问题修复、V1.0 发布 |

排期不包含多租户计费、原生多厂商协议和生产渠道自动处置。

---

## 18. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 模型随机性造成误报 | 结论不稳定 | 重复采样、动态探针、效应量、置信区间和最低样本门槛 |
| 安全策略被误判为隐藏注入 | 损害报告可信度 | 使用中性任务、可信基线、多语言差分和人工复核 |
| 模型自然提前结束 | Token 异常误报 | 持续生成任务、结构完整性、多档位与多轮统计 |
| 上游 Usage 不可信 | Token 证据不足 | 本地估算、流事件计数、证据降级并标注 tokenizer 不确定性 |
| 推理 Token 不可见 | 输出长度解释错误 | 区分可见输出和推理消耗，按模型能力配置适用规则 |
| 固定题库被识别 | 被针对性规避 | 模板参数化、nonce、同义改写和私有模板版本 |
| 检测成本失控 | 影响运营 | 运行前预估、硬预算、组织配额、提前停止 |
| 检测流量影响生产 | 稳定性风险 | 独立队列/Worker、目标限流、低峰调度 |
| API Key 泄漏 | 严重安全事故 | 信封加密、不可回显、日志脱敏、最小权限和审计 |
| 黑盒结论被用于法律指控 | 合规风险 | 风险化措辞、证据等级、免责声明和人工审批 |
| 规则升级导致历史分数跳变 | 趋势失真 | 规则版本冻结，区分重算分与原始发布分 |

---

## 19. V1.0 冻结产品决策

以下事项已经 M0-01 评审批准，V1.0 实现与验收必须遵循：

1. V1.0 交付多组织能力；首次初始化默认创建一个组织，所有业务数据和后台任务保持组织隔离。
2. UI 和报告统一使用“模型完整性风险指数”，不使用“掺水率”等确定性表述。
3. 完整响应正文默认加密保留 30 天，组织可配置 0～180 天；脱敏摘要默认保留 180 天。
4. 默认禁止内网 Endpoint；私有部署只能由管理员通过 CIDR allowlist 开放，并继续执行完整 DNS/IP、重定向和 TLS 校验。
5. 周期复检和异常通知列入 V1.1/P1；V1.0 支持手动重复检测和历史趋势。
6. V1.0 报告格式为 JSON 和 HTML；PDF/CSV 列入 V1.1/P1。
7. V1.0 的可信官方基线由各组织自行创建和审批，各组织自行提供凭证并承担调用费用；系统不内置跨组织共享官方账号。
8. 高风险结果只给出处置建议，不自动停用目标，也不改变外部生产渠道状态。
9. SQLite 可用于小规模单机部署；标准生产模式必须使用 PostgreSQL。精确单机并发和容量上限由正式性能验收结果冻结。
10. `RUN-008` 依据 MVP 功能验收要求纳入 V1.0/P0；探针复测不得覆盖原始任务或样本证据。

---

## 20. 发布门槛

同时满足以下条件才可发布 V1.0：

- P0 需求全部验收通过；
- 冻结验收集达到检出率和误报率目标；
- 完成 API Key、SSRF、越权和日志泄漏专项测试；
- 完成 100 并发和 Worker 故障恢复测试；
- 检测规则、评分权重和报告免责声明经产品、安全共同签字；
- 至少选择 3 个正常渠道和 3 个受控异常场景完成灰度；
- 运维具备队列积压、错误率、调用成本和密钥解密失败告警；
- 提供完整安装、升级、备份、恢复和回滚方案；升级失败时能够回退应用版本并保留检测数据。

---

## 附录 A：标准报告最小字段

```text
报告 ID / 任务 ID
目标名称 / Endpoint 脱敏值 / 模型名 / 渠道 ID
检测开始与结束时间
检测包 / 规则版本 / 模板版本 / 评分版本
总请求数 / 有效样本数 / Token 用量 / 估算费用
综合风险指数 / 风险等级 / 置信度 / 最高证据等级
四个一级指标及其子项
关键发现列表
探针统计及样本证据引用
基线信息和差异
失败、跳过和不适用项目
替代解释和检测局限性
人工复核结果
建议处置
内容哈希和导出时间
免责声明
```

## 附录 B：需求完成定义（Definition of Done）

一个需求只有同时满足以下条件才算完成：

- 产品行为符合 PRD；
- 接口和数据结构已更新到 Spec；
- 单元测试、集成测试和必要的安全测试通过；
- 有可观测指标和结构化错误；
- 敏感数据经过脱敏检查；
- 页面包含加载、空数据、失败、部分成功和权限不足状态；
- 文案不把黑盒统计迹象表达为确定事实；
- 变更已进入版本记录，并可安全回滚。
