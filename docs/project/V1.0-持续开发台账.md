# V1.0 持续开发台账

更新时间：2026-09-10。分支：`codex/v1-integration`。目标：完整、可运行、可复现验证的统一审核候选版；尚未完成。

## 当前有效状态（优先于下方历史 checkpoint）

本轮继续开发；上一轮为只读进度查询，不计新增开发交付。**a4f7487 / CI34449102328
已真实终态 failure：17成功、2失败、1取消**，本轮已重新从GitHub回读。以下旧的
“仍在途”状态仅为历史。PR6仍OPEN/draft、remote head仍a4f7487；main保护未变。

- Ubuntu打包私有日志断言误判已本地提交 **f213deb**：固定时间戳真红后，只对真实
  私有属性判泄露，保留日志时间和负面canary；当前routing+六父级双库三轮
  **20.266s PASS**（99593终态），纯表示100轮0.118s、SQLite父级3轮0.734s、lint0。
- quality另在原25分钟job总预算耗尽时取消；静态/构建/OS禁网均成功，不能忽略。
  已实现独立 **core-race** 任务及真实required绑定，不改原Core脚本、全量范围、
  双库或10分钟测试/25分钟job阈值。先红后绿 **194项策略、92项分片**通过、
  actionlint0；原生race/调度效果仍须新head新CI。见两个 M0-CI notes。
- M6执行维护Apply当前快组双库三轮 **90.746s PASS**（40504终态），包括同source
  并发、双审计头真实写锁和Load后撤权；原60秒/库自然失租组正在执行，尚未计通过。
  非执行Apply和app协调器继续专项集成，未提交WIP不能算完整备份恢复。

### 前一提交 checkpoint（CI在途描述保留为历史）

本轮已实际推进两项新恢复能力提交和两处共享原子性修复，不是仅重述进度：

- **9757f8f / c90f18e** 连同先前四个功能提交、证据 checkpoint **a4f7487** 已推送。
  PR6 实际 OPEN/isDraft=true、head `a4f748725b2cbfc98978f6298f3502cf2af17460`；新
  **CI34449102328** 已实际 in_progress，最近回读9成功、10活跃、尚无失败或终态。
  沿该run继续核验，不重启；以下旧ce8的20绿不追认本批。
- **c3c757b** 本地提交普通 q.reconcile 精确终止UPDATE1，修复旧Job仍running却提交
  预检失败投影的缺口。实际普通Claim两类SQLite真红0.444s；完整 `TestJob|TestPrecheck`
  **双库三轮39.835s PASS**（18160终态），原取消/耗尽/UNCERTAIN和历史actor不变。
- **c3a092c** 本地提交审计事件INSERT和链头UPDATE精确1。八类实际SQLite真红0.858s；
  修复后新旧审计/20路并发/敏感变更/篡改/健康检查 **双库三轮32.652s PASS**。
  原维护admission五项+pause+domain **40.337s PASS**，原并发迁移/初始化 **2.242s PASS**，
  均真实双库三轮；初始化失败零结果、业务/审计/链头全回滚。独立只读复核无确认P1/P2。
  Windows vet、完整repository lint0和Linux交叉vet0，不等于Linux原生/race。

原 q.reconcile 与审计 UPDATE0 缺口均已按上述证据修复，不再列为待实现。两个最新
修复尚未推送、不受 a4f7487 CI 覆盖；待该运行实际终态后另推送并检查精确新head。
普通PG组合第一次命中其他execution文件落盘间隙而编译失败（8a0ac4），未进入DB，
没有删除并行文件或追认为通过；完整helper落盘后才执行上述实际双库验证。

完整执行类和非执行类维护Apply已进入实现/SQLite失败路径阶段，仍未完成全部
TLS/MAC、双库、真实权限/lease及协调器接线。root全部DB命令已终态，General明确
独占交给execution单元；root/terminal单元不并发PG，Backup未操作。以下六项单元
证据保持有效，后续WIP不计入它们的提交/CI。正式审核和完整77需求/91任务不缩减。

### a4f7487 已推送单元与证据

上一轮只读进度查询取得明确在途 session59941 的成功终态，并回读 PR/CI；属于
已核验等待，不计新增功能。本轮已将其验证的两项能力分别实际提交，继续推进实现。
**ce8dbef / CI34441453034 已实际终态 success，20/20 jobs 成功**，root 再次回读确认。
仓储 race 分片已有该批原生 CI 证据；该结果不覆盖下列新提交和下一阶段 WIP。

本批六项功能提交及实际证据：

- **0385061**：原 rule/template 资源依赖观察；完整原字节 streaming、未知历史 opaque、
  配置/参数/载体 hash 分离、真实模板成员匹配。bundle/manifest/contracts 三轮
  **1.197s / 3.367s / 1.480s PASS**；不是完整 collector/恢复。
- **2b81f9f**：真实维护权限下的候选观察及安全过期任务暂停。fast31 双库三轮
  **67.170s PASS**；原生产60秒 lease自然到期两库各一轮 **120.951s PASS**，验证
  末端失权全回滚及另一 Store takeover。root完整SQLite drain/admission **64.603s PASS**。
  PG首次69.882s为已修复的专有fixture问题，未放宽生产期限/历史/权限检查。
- **26ee5ab**：共享执行结算核心与真实 UPDATE0/批次部分更新漏洞修复。完整
  `^(TestExecution|TestDerivedExecution)` 双库三轮 **275.069s PASS**，包含1000样本、
  原子故障、取消和预算；Windows vet/lint0、Linux交叉vet0，不冒充原生race。
- **625574d**：Run/Estimate/Baseline 原输入估算器身份；未知/缺失独立 incomplete，
  不补默认历史。完整引用/兼容/result/库存组合双库三轮 **184.972s PASS**。
- **9757f8f**：只持有 Builder/派生sealer 的恢复准备入口，普通reconciler复用。
  新旧准备/真实TLS reconciler全匹配双库三轮 **7.774s PASS**（59941 第一命令）。
- **c90f18e**：分析/报告/预检的同事务终态共享核心，固定标量读、历史actor不变、
  实际选中行精确UPDATE1。新旧完整相关父级组合双库三轮 **95.544s PASS**
  （59941 第二命令，a5936c终态0）。独立全文只读复核无本次新增P1/P2。

root另以独立 detached **625574d** 检出隔离主树WIP，真实TLS/MAC恢复双库三轮
**6.025s PASS**，bundle/manifest/contracts三轮 **1.206s / 3.511s / 1.032s PASS**，
仓储/Worker/bundle vet/lint0（47393终态d369f2）。临时副本确认精确HEAD及零改动后
已清理（eb24b3），主树和全部提交保留。新两单元已全文复核所有生产、测试及notes。

下一阶段正实现维护权限下的完整 execution Load/Apply：同事务原Job终止、S1与
UNCERTAIN计费及域结算；不生成普通consumer、不重发HTTP、不补历史身份或MAC。
独立复核另发现普通 q.reconcile 的旧 terminal Job UPDATE 仅检查Error，正用真实
Claim+触发器回归处理；不将可信核心测试替代普通调用者原子性证明。General59941
已终态，无本地DB命令在运行；下一独占仍由root调度，Backup未操作。

上述六单元已随a4f7487推送，新CI仍在途，PR6仍OPEN/draft；不修改main保护或PR5。完整drain、
资源收集、真实完整capture/恢复与CLI/API/UI仍未完成。M5余项、完整M6、M7/M8和
SEC/ALG/OPS原77需求/91任务不变；正式审核待统一，M0-01～03批准保留，Goal未完成。

### ce8dbef 已推送 checkpoint（以下在途CI描述为历史）

本轮已提交 **a0dbce7**（仓储 race 进程分片）及 **33f13e9**（真实私有归档构造/
发布→完整读回→完成事务），连同checkpoint实际推送 **ce8dbef**（完整head
`ce8dbefb09fecc3b7940d3151bae6022fde74f9f`）。PR6仍OPEN/isDraft=true，实际新
**CI34441453034已in_progress**，实际2成功/17活跃/无失败，尚无终态；只追踪该run，
不追认旧失败或未提交单元。
上一轮进度查询实际观察了仍活跃的CI，不计新增功能；本轮有实际实现、反例和提交。

原 **9765605 / CI34439268496 已终态 failure：18成功、2失败**。Windows打包、quality、
其余race组和native PG均成功；repository-race-2包总600.044s触及10m上限，其当前
SystemStatus/sqlite仅7s，required随之失败。没有确认data race，也没有重启旧CI。
新分片保留六个required jobs、每进程-race/-count=1/-timeout=10m、完整双库与父级
子树，改为每仓储job两个顺序子组；不改25m job上限。离线先红后绿 **92项**、
M0-04策略 **165项**通过；真实非race RE2枚举仓储456/十二选择、Worker75/六选择
无漏/重复。不是原生race执行；修订效果须新CI证明。原被中断SystemStatus用例
另实际双库三轮 **5.614s PASS**，不冒充race耗时证明。详见对应分片notes。

归档发布新增单元已全文复核生产、所有测试与native夹具。Windows原Temp祖先权限
真实红，只修专属随机私有测试目录，未降低生产检查。workspace与密文发布顺序、
共享对象两种累计预算、全文件认证、取消等待、晚失败保留orphan均有实际测试。
root专属成功/真实logout/末端SQL回滚双库三轮 **12.558s PASS/4786**；完整publication
前缀含SQLite/native反例与DB-aware双库组三轮 **48.692s PASS/60214**。不是每个
纯文件反例都单独重复PG，也不是完整capture/恢复。所有库存payload明确synthetic。

为排除其他单元WIP影响，root创建独立detached checkout验证实际 **33f13e9**，未隐藏/
删除主树改动。新旧app **全部TestBackup前缀三轮、实际提供双库DSN：125.255s PASS**，
contracts三轮 **0.904s PASS**、app vet/lint0，**39657已终态0**。独立副本确认原head和
零改动后已用git worktree remove清理；主工作树与所有提交/他人改动保留。主树前次contracts
三轮0.803s及app vet/lint0也通过；新单元Linux只交叉编译/vet，原生与race等新CI。
详见 M6-BACKUP-PUBLICATION-NOTES.md；不是整app全包或完整备份产品验收。

未提交的下一阶段仍由各单元推进：完整候选观察/安全expired Job暂停（不确定请求
必须另原子结算，尚不能称完整drain）；Run/Estimate输入估算器的原历史身份；原
rule/template到资源依赖的有限解释。两份已全文核对的drain/剩余闭包设计仅是设计。
General已由root释放并独占交输入引用单元，其他人不并发PG，Backup未操作。
M5其余、完整M6、M7/M8及SEC/ALG/OPS仍按原77需求/91任务推进；正式审核继续待统一，
已有M0-01～03批准不变，完整Goal仍未完成。

### 9765605 已推送 checkpoint（下方CI运行描述为历史）

本批已经实际推送 **9765605**（完整head `976560519010fd8f837b558936636511a5d4367a`），
PR6仍OPEN/isDraft=true；**新CI34439268496已实际in_progress**，无终态结论。
按同一run继续观察，不重启或用前一20绿追认。后续publication与drain/closure设计为
下一单元，未提交内容不受这个CI覆盖。五个功能提交及各项实际证据如下。

本轮继续实际集成；上一轮只读进度查询确认 session43990 已终态通过、并重新确认 CI，
属于取得具体在途验证终态的已核验等待，不计新增功能。
**27caa17 / CI34435434812 已实际终态 success，20/20 jobs成功**，root 已独立回读；
Windows/quality/required 均通过，不覆盖本轮未提交代码，也不证明旧稀发 HTTP deadline 根因已定位。

新增真实备份完成生命周期与 migration22：CompleteBackup 最终权限/fence/阻塞检查、
不可变完成receipt、v2完成事件/审计、正常态解冻同事务；原21迁移与v1事件字节不变。
新单元真实双库三轮 **31.201s PASS**，新旧完整maintenance组合 **155.943s PASS**，
含真实60秒lease到期/第二实例takeover。独立只读复核无阻断发现；不是完整备份恢复。
见 M6-BACKUP-COMPLETION-NOTES.md。实际归档读回到完成事务的 app 接线已收口，见下方新证据。

Result全部历史revision引用单元已冻结：agent真实双库三轮 **43.655s PASS/schema22**，
Windows/Linux vet/lint0。root新三错误外层映射纯回归先红 **0.098s**，随后实际接线；
root结果引用/新旧PG外层组合三轮 **106.165s PASS /session62095 terminal0**。
上述仓储完成单元已提交 **451ab26**；Result 历史引用及真实 PG 外层已提交 **c6e636b**。
状态查询联调发现 CreatedAt 未绑定原 begin 的真实缺口，修复共享验证后，完成及该反例
双库三轮 **49.039s PASS**；全部新旧 maintenance 再跑 **159.994s PASS/session29273**。
没有改旧 v1 canonical 或已应用迁移，不用创建时间合法正数代替审计来源。

私有可复读 workspace 已提交 **3660a2c**，root 完整复核全部生产/native/测试；
privatefile 全包三轮 **4.676s PASS**，Windows privatefile/repository vet/lint0、Linux cross
vet0（19332终态0）。独立只读复核无阻断；不是 Linux 原生/race 或完整恢复证明。

app 实际文件读回→AEAD→精确 manifest→CompleteBackup 与新增测试文件已落地，
SQLite 专项三轮13.903s；root 新旧配置/报告/完成组合三轮 **14.541s PASS/session98321**，
未提供 General DSN，该次不算 PG 证据。独立只读复核无阻断。全部 payload 明确为
合成夹具，仅证明真实文件/加密/完成事务组合，不代替完整快照和恢复演练。
root 又补实际归档 manifest v2/v3 scoped/legacy-empty/missing/unmapped 组合；正确 AEAD
但把原 observed-empty 换成非空仍不完成，SQLite 三轮 **1.935s PASS**，不是完整库存证明。

操作授权状态/列表及共享 gate 的 SQL NULL 分类修复已提交 **5c89286**。原 PG driver
子路径命令真红 **201.012s**，且发现其遗漏 mode/driver 嵌套子树，均没有追认全绿。
修复后无 driver 过滤的全部快路径双库三轮 **38.825s PASS/99317**；独立自然60秒慢组
两库各一轮 **120.917s PASS/18475**，覆盖全部实际层级。历史状态 ID 排序不是时间序。
root 在最终 NULL 修复上重跑原 SystemMaintenance+BackupCompletion SQLite整组
**66.917s PASS/13807**；repo/app Windows vet/lint0、Linux cross vet0（4599终态0）。
完整归档完成组含v2/v3、所有嵌套故障的实际双库三轮 **65.945s PASS/session73895 terminal0**。
app 单元已提交 **ddf59fc**，不是完整 capture/恢复；专有 notes 明确 synthetic 库存范围。
General 已由 app 明确释放回 root，目前没有本地 DB 命令在运行；Backup 未操作。

contracts 三轮 **1.099s PASS**；M0-04 policy **165项通过**，race分片离线 **87项通过**。
本机 NativeGo race 枚举实际失败，进一步原命令证实 `-race requires cgo`；CGO=0，
不算race通过、不改CI门禁，新批原生/race仍由新CI验证。
最终 contracts 又三轮 **0.847s PASS**。本 checkpoint 包含上述五个功能提交与完整证据；
推送后必须按本批实际 head/run 检查新 CI，27caa17 的20绿不覆盖它们。

下一工作已经分工：实际归档构造/工作区完成后密文发布接线、冻结下保守drain设计、
剩余资源根闭包核对。新单元未验证/未提交内容不得纳入本checkpoint或用CI追认。
M5余项、完整M6、M7/M8及SEC/ALG/OPS仍按原77需求/91任务推进，不缩小Goal。

### 前一已推送 checkpoint（下述运行状态为历史）

本轮已实际推送 **27caa17**（含f305a92报告文件、1ab6e13历史引用与文档），PR6仍
OPEN/isDraft=true。新 **CI34435434812 /27caa171ad6623a6d6cc9b64a2a19ff0f81a21c5**
已实际 in_progress，尚无终态；按同一run继续观察，不用上一20绿追认。未提交的
snapshot_result_references* 是下一单元，尚未完成验证，不属于本次CI源码。

上一轮为只读进度查询，确认同一 CI 当时仍在运行，不计新增功能。本轮继续实际开发。
**46ce663 / CI34433191181 已实际终态 success，20/20 jobs 成功**，root 已独立回读确认；
这不证明旧 Windows 稀发 HTTP deadline 的底层原因已经定位，也不覆盖本轮新增代码。

现代报告实际文件复制已提交 **f305a92**：复用真实安全 Store.Read，JSON/HTML/CSV 原值、
16 MiB 上限、64 KiB 输出、失败零收据/清零、真实硬链接拒绝和 AEAD 末尾认证失败均已测试。
root 完整复核，与既有安全配置模板组合三轮 **1.593s PASS**；不是完整备份/恢复或发布入口。

Run/Estimate/Baseline 引用观察及 PG 外层已提交 **1ab6e13**：agent 双库三轮49.777s；root 新旧
库存/外层三轮75.780s。root 另实测最后一次结果读取后 rollback 仍返回成功：双库真红
0.745s，补同事务末尾 count 后三轮1.957s通过，全部 Snapshot/Migration 单轮149.426s通过。
独立复核发现合法 Unicode model 被128字节误拒，修后真实双库三轮11.804s通过；另旧 SQLite probe ordinal 的
int64合法范围被错误收紧到int32，纯红0.127s后修复，真实原writer双库三轮2.194s通过。
最终全部原引用、模型兼容、ordinal、末尾rollback及PG外层组合三轮 **83.168s PASS**
（2604终态0）。第一引用是根发现单元，不等于所有result/S1/资源的完整引用闭包。

下一阶段所有 result revision 的资源引用已开始；并行新文件尚未完整落盘时曾使 contracts
及模型专项在编译阶段失败（缺 snapshotResultReferenceDocument），没有运行到数据库或
功能断言；不能记为通过。后续已补齐可编译代码，但自身测试与35项lint诊断仍由该单元
修复，不在本批提交内。General 当前已交结果引用单元，root 无DB命令；Backup 未操作。
README 已纠正仍称 M0-04/Worker 骨架的旧描述及 SQLite 错用独立 server/worker 的启动指引。

为避免在建文件干扰已提交版本验证，root 创建明确的 detached临时worktree验证1ab6e13，
未删除/隐藏主树WIP。第一次检查早于checkout终态而vet缺web包；等待83634真实完成后
重新执行带逐步失败退出的完整检查，**711aff终态0：repository/app vet无诊断、lint0，
引用纯组合三轮0.418s、报告文件/配置三轮1.312s PASS**。验证副本确认无修改、身份正确后
用git worktree remove清理；源码提交与全部主树改动仍保留。contracts三轮1.286s及
M0-04离线policy165项通过；没有改分支保护或重做已完成的M0修复。

完整目标仍为原77需求/91任务，M5/M6其余、M7/M8、SEC/ALG/OPS均未缩减；正式审核统一
待全部开发完成，现有M0-01～03批准保留。当前仍未完成完整候选版。

### 前一 checkpoint（历史，以上方实际终态为准）

本轮checkpoint已实际推送 **46ce663**，草稿PR6仍OPEN/isDraft=true。新
**CI34433191181** 已实际进入运行；root观察器84666因TLS handshake timeout结束，
不代表CI终止，改按同一run查询，不重启workflow。最终结论尚待实际终态。旧
CI34430463313的18成功/2失败保持真实历史。未提交的新工件引用/现代文件复制单元
不受该CI覆盖；General当前归引用单元，Windows agent仅观察CI。

本轮已本地提交 **2eaa945**（历史key/原行SQL/外层接线）、**16c6613**（完整ready
报告库存）、**d324013**（安全配置模板）及 **5269782**（HTTP超时前闭集诊断），
将随本checkpoint推送。新CI按真实运行身份另行跟踪，不使用旧通过结果追认。

前一推送 **57c5571 / CI34430463313** 已实际终态failure：18 jobs成功，
Windows package及required失败。Windows首轮全量Go通过（app56.258s、repository
442.281s、worker137.781s），第二轮webassets/app报告状态GET触发原5秒客户端
deadline；当次仅有listener日志，没有旧Claim错误，真实等待层尚未确定，不沿用
旧根因。原命令本地后续一次webassets app26.815s通过不等于修复。诊断已提交：
原失败用例自然五轮53.367s、新观测版30日/0日真实双库三轮115.035s通过，仍未
复现该deadline。root要求加强stop/join观测后纯诊断20轮0.687s通过、vet/lint0。
上一轮主要为只读进度查询，不计新增开发；本轮继续实际集成并取得以下新证据。

key最终修正 envelope顺序/空白过拒、合法空audit head过拒及Unicode SimpleFold
别名检查缺口后，实际双库专项三轮 **64.732s PASS**，Windows/Linux交叉vet/lint0。
legacy SQL原行读取实际双库三轮 **41.574s PASS**。root六个新错误类闭集映射真实
红0.089s→三轮0.100s/0.098s后，真实PG包装/吞错/sticky/服务端42704与新旧库存
组合三轮 **135.530s PASS**（23589终态0）；全部Snapshot/Migration组合单轮
**125.375s PASS**（5348终态0），repository vet/lint0。这两个完整新增单元及外层
接线已提交 **2eaa945**；仍不是完整密钥认证/备份恢复。详见对应notes和集成notes。

**16c6613** 已提交现代/历史完整ready报告库存：真实双库三轮 **40.134s PASS**，
root全文复核和纯格式三轮0.104s通过。历史原行保留unverified/unmapped，不能自动
降级现代损坏或当作文件已复制；实际文件映射/归档仍待接线。

安全配置模板载体 **d324013** 已提交：只取实际Config的role/driver/key版本，其他字段固定安全
迁移占位，保留Config/KeyFileReference禁止序列化规则；真实LoadConfig四种模式、
全部64key、篡改/别名/限额/取消、真实AEAD后期认证失败均有测试。最终新旧配置
组合三轮 **0.754s PASS**、Windows vet/lint0、Linux交叉静态0，fuzz5636次/6.308s。
独立复核无P1/P2，末尾失败阶段断言已补强。不是完整归档或恢复启用。

本轮追加 contracts 三轮 **1.136s PASS**，M0-04离线策略 **165项通过**；原两项
修复2238e68仍有效，不重做、不变更远端分支保护。正式审核状态与既有批准保留。

下一单元：工件引用观察/完整闭包、实际报告文件/配置归档及备份恢复协调器。
General已由Windows诊断明确释放后交引用观察单元独占；root仅做纯层/静态测试，
另一个单元只做现代报告实际文件复制，无DB并发。Backup未操作。正式审核仍待全部开发完成；原77需求/91任务、
M5其余、M6其余、M7/M8及SEC/ALG/OPS完整范围不变。后续checkpoint不覆盖当前状态。

本轮继续 Goal 实施；上一轮只读进度查询取得新 CI 终态，改变了下一步修复依据，
不是新的功能交付。**8fe207a / CI34427119645 已终态 failure：18 jobs成功，
Windows package及required失败**。实际第二阶段 webassets/app 的 zero-day SQLite
故障 Writer 测试中 Worker claim 报 database_unavailable 并退出。没有把旧 CI 追认全绿。

本轮已提交：**1614b34** tokenizer完整词表/许可载体及归档重建，
root完整包三轮 **11.424s PASS**、vet/lint0；**3e31fd8** 规则/模板原字节复制及
七类PG外层错误映射，root真实双库新旧artifact组合三轮 **33.823s PASS**、vet/lint0；
**e03a161** manifest v3与原20列行摘要（绑定NULL/方言/时间原表示），root完整
manifest三轮 **3.405s PASS /94.7%**、vet/lint0，真实AEAD组合保留原v1/v2字节。
详见各 M6 单元 notes；这三项不是完整备份恢复或历史报告已恢复。

Windows夹具修复已实际复现：故障Writer内的全历史审计占用live Store单SQLite连接，
旧barrier路径 **2.214s FAIL**；独立物理RO观察保留首字节前完整审计/收据绑定，
barrier三轮 **2.034s PASS**、zero-day webassets五轮 **36.367s PASS**，原CI形式
web/app全组SQLite **27.637s PASS**。已补早退cancel/release/join及纯反例；root最终
真实双库完整webassets/app **59.660s PASS**，PG observer实际覆盖、vet/lint0。
已提交 **6507c02**，仍不宣称CI唯一根因或生产容量完成，新远端CI须独立确认。

**2a9abfe** 安装评分资源载体及真实归档参数重建已提交：补充中途取消后完整bundle
三轮 **0.444s PASS /88.0%**、vet/lint0，两个同名租户候选参数保持不同；独立复核
无阻断发现。bundle/manifest/tokenizer/tokenrisk/report/contracts组合三轮均通过。
key inventory与真实legacy行SQL适配器两个独立新增单元正在实施，尚未集成验证。
General DB当前独占给job_inventory的key测试；root/legacy SQL agent不并发PG，
Windows修复agent已交付后转只读复核key单元。旧checkpoint的占用声明不代表现状。

完整范围保持原77需求/91任务，Goal active；下一步是新CI、引用闭包、真实报告文件/
配置归档、备份恢复协调器/隔离启用/CLI/API/UI与干净环境演练；M5余项、M6其余、
M7/M8与SEC/ALG/OPS均未缩减。正式审核仍统一在全部开发完成后进行。

### 本轮推送前 checkpoint（历史，当前状态以上方为准）

本轮接续现有代码而非重做：上一轮只读查询确认 **0268a09 / CI34334609860 已终态
failure**，18 jobs成功，Windows package及其required总门禁失败。image已success，
因此022825a的Docker CSV输入修复获实际新镜像验证。Windows原生数据库Unavailable
原因尚未明确；app退出后HTTP失败是后续症状，repository为包累计600秒超时，不能
把当时刚开始的清理子测试说成死锁。本轮由此进入实际修复和集成，非仅重复状态。

新增已提交（尚未推送）：**f4fa833** 完整构建测试包顺序执行、强制count=1，保留
全部用例/包内并发/原期限/失败门禁；PowerShell AST检查真红后34例通过，原policy165/
race87及contracts三轮1.164s通过。调度是待实际新CI验证的缓解，不冒称底层根因已修。
**3f2b9d0** 真实同快照报告清单；全专项双库三轮35.637s，最后Close/Open consumer
改动专项三轮12.408s，root复核、Windows vet/lint0。保留101/102跨页/跨连接/禁用
组织、末项损坏零结果、第二正文取消、最大合法内核输入等证据，不等于完整备份。

新增已提交：**4017622** Job/Attempt 完整库存，实际双库三轮48.138s；
**66c0f3b** 六项闭集错误的PG外层集成，真实错误/吞错/sticky/服务端42704失效；
**bc04127** 报告故障夹具cancel-and-join，纯层真红后修复，真实双库三轮29.694s。
root 新旧报告/Job/迁移/审计/PG快照广组合三轮 **195.166s PASS**（51154终态0）。
上一轮只读进度答复取得该终态证据，本轮据此提交这三个单元，不计为已完成完整备份。
当前启动完整 `go test -p 1 -count=1 ./...`（94627）：实际出现tokenrisk内部测试经
secret→repository→report→scoring返回自身的import cycle，已是真实全仓失败，
其它包继续收集结果。**2291bac** 跨模块测试已迁移到外部test包，保留原断言和生产分层：
完整tokenrisk三轮0.700s；root tokenrisk/report/manifest/contracts各三轮
1.213s/4.494s/3.862s/1.392s通过，全仓vet0/lint0；该次已失败的94627仍不追认通过。
新组合仍待完整回归及新远端CI，不使用旧20绿或专项绿追认。
当前General测试实例已核验原state并启动（PID7688/15432），经历真实WAL恢复后ready；
未重建/重置实例、未改配置、未操作Backup。当前DB由root完整回归独占；报告/Job
agent仅做下一单元只读设计，Windows诊断agent仅修Token测试依赖，不运行DB。
旧checkpoint中的agent/PID/占用声明不再代表现状。

下一步仍是完整inventory（所有规则/模板/安装资源及根密钥引用闭包）、实际报告文件/
配置归档、备份恢复协调器、隔离恢复/激活、CLI/HTTP/UI和真实干净环境演练；M5余项、
M6其余交付、M7/M8与SEC/ALG/OPS保持原77需求/91任务。Goal active，正式审核统一
待完整开发完成；不得把合法旧报告当前Unsupported边界当成永久缩减历史兼容范围。

### 2026-09-09 提交 checkpoint（历史状态，以上方为准）

最新提交收口：**2357652** CSV 产品全链已提交/推送；**ee418db** 同快照迁移历史
及 PG 外层闭集错误；**d19582d** manifest v2 多租户工件身份；**022825a** Docker
CSV 测试输入修正。后三项准备随此 checkpoint 推送。不是 Goal 完成。

CSV 新CI34333125156的image已实际失败，job日志定位唯一契约错误：backend stage
缺少 scripts/read-contract-schemas.go。未改测试为skip；新增配方回归先红0.075s，
补精确COPY后完整contracts三轮0.851s、vet/lint0。quality与Windows在最后观察时
仍运行，其它已完成项含Linux包、全部双库race、PG native均成功；不称整轮通过。
本机无Docker，022825a必须由新CI实际镜像构建验证。新推送可能按现有concurrency
策略终止旧运行，运行终态以GitHub实际状态为准，不人为追认旧或未运行步骤。

迁移库存root广组合（全部迁移/PG snapshot/全组织audit inventory/完整audit snapshot）
真实双库三轮 **67.211s PASS**，Windows/Linux目标静态0。原错误归一真红0.089s后
修正，真实PG失败/吞错/包装及服务端42704失效均已验证。只证明history，不认证物理
schema。manifest v2 root完整复核后全包三轮 **3.079s PASS / 93.5%**、vet/lint0；
v1原固定哈希不变、跨组织同版本不同字节、真实AEAD+VerifyStream、65536项/16MiB
反例覆盖。contracts三轮0.814s、race策略87通过；这些不替代新CI。

当前下一工作：pg_native_process实现报告快照inventory，独占General DB；
audit_snapshot_finish实现Job/Attempt快照inventory，暂不运行DB；root持有Git并管理
CI/最终接线。只有专有新增文件，不覆盖已提交CSV/迁移/manifest。Job历史schema7/17
确有NULL job_id/G0和PLANNED合法遗留，以及retry后sample.job_id改变；须保留完整
计数与私有legacy_incomplete分类，不能误判全部旧历史损坏、丢弃或补写。观察成功
不是发布或恢复激活授权，legacy处置仍须纳入完整协调器。Goal继续原77需求/91任务，
M5余项/M6完整备份恢复及交付/M7/M8/SEC/ALG/OPS不缩减，正式审核统一待开发完成。

### CSV 提交前 checkpoint（保留证据，提交状态以上方为准）

本轮最新：上一轮仅回答进度，不计新增交付。本轮完成 CSV 产品联通收口：
生成/仓储/真实 Worker/HTTP/前端/OpenAPI 接线，三格式应用真实双库、30日/0日
完整链各三轮 **111.340s PASS**；最新 bundle 真实浏览器新建 CSV 修订2、旧三格式
下载校验、机器分数保持、退出，然后同一应用继续全部清理/报告/S1/审计断言，
**288.417s PASS**。首次浏览器后旧夹具要求两条报告的306.782s失败、应用 QF1003、
前端旧文案断言929/930失败均如实修复并保留证据；最终前端 **930 tests PASS**，
typecheck/lint/build通过。root 全仓 vet/lint和相关包复验0，最终contracts三轮0.851s。
CSV整体尚待本轮提交/推送新CI；REP-007更新进行中，PDF和全部原剩余范围未排除。
详见 M5-CSV-INTEGRATION-NOTES.md。

远端 **9ad2e68 / CI34329687200 已完成全部20 jobs success**，包含真实 Linux
pg-backup-native、quality、Windows/Linux打包、镜像、所有race及required；不用它
追认未提交CSV或后续inventory。已开始同快照迁移历史inventory与manifest v2租户
工件身份两个独立单元，尚未集成提交，不声称完整库存/备份/恢复成功。Goal active。

### 以下保留本轮之前的 checkpoint，现状以上方及最新代码为准

最新本轮终态：独立Backup cluster接线后，原完整audit/snapshot/全部Dump组合三轮
**108.814s PASS**，含真实26MiB导出/新库恢复、非超级用户双会话隔离、控制连接
恢复、2秒取消和全部10秒DROP清理。Backup复查无fixture库；旧General尚有精确
残留mii_dump_src_4047502431173381134（INVALID/current owner/0会话），本轮未删除。
root完整pgbackup tagged三轮22.959s PASS，Windows/Linux目标tagged vet/lint0；
Linux原生仍待新CI。已提交be5da74 manifest测试依赖修正、f2d5cde CSV生成内核。
CSV上层仍集成中，不据内核通过标记REP-007完成。PG工具/会话/隔离/专有CI完整单元
已提交55435db（23文件）；正在准备推送新CI，尚未获得新远端通过结论。

本轮继续开发：上一轮只读回答进度，不计新增交付。随后实际将原General与Backup测试分为固定独立cluster，新Backup在原不存在的 `.tools/test-postgres-backup` 初始化并成功启动（loopback15433）；原General进程26484保持运行，无重启或配置变更。root复跑50项实例策略通过，实际只读设置为PG180006/fsync=on/synchronous_commit=on。原组合三轮161.907s FAIL（共享实例DROP等待检查点）的证据保留；新的完整同范围三轮正在运行，尚无终态。

Backup tagged fixture现强制独立 `MII_TEST_PG_BACKUP_DSN`，当General同时配置时实查两端 `pg_control_system` 身份，另一种URL写法指向同一cluster仍拒绝；CI新原生job已同步新环境变量，旧通用race环境不变。新35项原生job变异契约包含错用旧DSN，root全contracts三轮0.645s、CI策略165、race策略87、actionlint通过。Linux原生仍待新提交/推送/CI，不使用ed446fb旧19绿追认。

CSV内核由root完整复核，整个report三轮4.014s PASS；manifest测试循环修正完整三轮0.312s PASS，report/manifest/pgbackup vet/lint0。CSV仓储/存储、Worker/API、前端/契约三个独立上层单元已开始，尚未产品联通验收。原77需求/91任务、M5其余/M6其余/M7/M8与SEC/ALG/OPS未缩减，Goal active。

最新实际收口：`3186c3a` 已提交原生工具进程边界（9文件）。root全pgbackup含native/Dump纯层/CRL/真实TLS三轮22.307s PASS；相关tagged仓储/pgbackup vet/lint与actionlint均0。原Job计数失败未虚构唯一OS根因；最终修正显式固定uintptr ABI缓冲、同原5秒期限重取不一致清单，原并发数/1024容量/完整终态要求不变，强GC30轮亦通过。Linux原生尚待新CI。

root原三个实际PostgresDump top三轮 **16.690s PASS**（session70217）：26MiB同快照导出/新库恢复/迁移/每行数据/审计锚点、精确schema与三类失败、2秒取消与实际服务端Lock清理，所有DROP cleanup通过。夹具仅调整为所有源断言完成后先Close/精确DROP src，再创建/恢复dst，保持原10秒DROP期限、fsync和server配置；不是放宽阈值。5个旧owned dst经重新核验INVALID/current owner/0会话后精确逐个DROP，2.291s成功并复查精确集合0；无业务库改动，内容可由夹具重建。

PG生产/仓储接线仍未提交：root已修外层闭集错误被归一（0.086s红→三轮0.103s绿），再修ctx取消吞掉Cleanup/Unseen/Identity的不确定状态（0.087s红→两top三轮0.088s绿）。独立复核发现有效view前置参数失败未记sticky，专有agent已修且待新实际验证；另正在补同Store池一次、原1500ms总预算内的控制连接恢复，以及真实已绑定Lock后的control.Close故障。原三轮通过不追认后续修改。当前DB由postgres_snapshot_review独占，root不并发。

完整范围继续推进：独立开始M5-08/REP-007 CSV软件导出内核，限新增report/csv及专有tests/notes，保持JSON/HTML既有字节/hash/S1范围不变；持久化/API/UI尚未接线，不能标记CSV或REP-007产品完成。root本轮contracts三轮0.906s、CI policy165、race分片策略87通过；新Linux原生CI方案仍未提交/运行。原77需求/91任务、M5其余/M6其余/M7/M8与SEC/ALG/OPS范围不变。

本轮继续实际开发，新增提交 `7b4b6cc`：Windows PG18.6 本地测试运行库配置、真实性/路径/无覆盖/版本探测及15项真实回归；root重新完整读取三文件并实际运行15项1.870s PASS，DLL与来源记录仍只在ignored .tools，不修改系统目录/PATH/DB，不构成生产再分发授权。root新增ApplicationName限定nonce后的Dump/CRL纯层三轮0.790s PASS。前一轮主要为只读进度答复，但取到了先前测试的真实终态和闭集检查点证据；不计新增功能交付。

当前pg_dump仍为未提交WIP，不能使用此前native三轮通过追认：root后续native三轮20.267s FAIL，实际在10并发预热返回ErrProcess；专有agent100轮62.694s复现4次，闭集阶段为Job清单assigned/count不一致，正在原5秒期限和1024容量内修正并重验。PG服务端会话协调器已落盘、仅tagged compile通过，正在真实验证精确nonce/PID/backend_start身份和取消后清理；无完成声明。

最新真实RoundTrip session92056已终态13.986s FAIL：数据/迁移/同快照审计锚点断言走通，唯一dst DROP在10秒cleanup期限失败，内嵌采样为CheckpointDone:187。对应server.log显示此次DROP自身的immediate force wait checkpoint共10.140s、sync9.193s/382文件，不是沿用旧235秒检查点猜测；测试夹具清理尚待修正。首轮4个精确核对为INVALID且无会话的自建残留库已逐个DROP成功，未影响源业务库；后续失败产生的新测试残留尚待精确清理，不能将旧“4个尚未删除”当现状。当前测试数据库时段交postgres_snapshot_review，root不并发DB。

本轮实际提交 `8ad33ed`：同一快照全部组织/整链审计清单，含分页、孤儿拒绝、初始化/分段/容量边界和PG外层闭集错误映射。root先真实复现未初始化错误被归一丢失（1.406s FAIL），修后完整新旧快照组合双库三轮54.349s PASS、仓储vet/lint0。原77需求/91任务不变；上一轮只回答用户进度，不计新增交付。

真实pg_dump单元尚未提交/完成：已实现显式环境/固定18.6/有界输出/Windows原子Job与Linux进程组，Windows native三轮19.598s PASS。已定位最小环境下缺VCRUNTIME140.dll，新增专用工具脚本以已安装Microsoft签名、版本/hash固定的DLL仅配置ignored .tools app-local，未修改系统PATH/System32或DB，且不构成产品再分发授权。真实TLS三轮3.495s PASS，包含撤销叶/中间CA、坏CRL负控和显式空CRL；Linux对应证据待新CI。

root新增显式tag的真实新数据库测试首轮59.657s FAIL，必须保持未完成：26MiB全库dump→pg_restore新库→原数据/迁移/审计锚点比对、精确大小写schema与限额/写失败/错密码均走通，但前两tops各自两个10秒DROP cleanup失败；第三top真实观察数据库端Lock后取消，原生dump已在2秒内退出但服务端Lock未及时消失。4个仅本轮自建的src/dst库残留，暂未删除；无源业务库被覆盖。session65504已terminal，root持有DB；audit_snapshot_finish只读诊断cleanup、postgres_snapshot_review只读研究精确服务端取消。不能把body部分成功或进程退出当完整备份/恢复成功。

新增required pg-backup-native CI方案已落盘未提交，保留旧19任务并增加带init的固定PG18.6容器原生dump/restore/TLS/process测试；离线policy165、race策略87、contracts三轮0.667s、Windows/Linux目标vet/lint/actionlint通过。上一CI34319259511 / ed446fb已由实际run终态确认全部19 jobs/steps success（quality17m41s、Windows12m04s、Linux5m37s、image8m30s）；watch42683仅因TLS观察失败终止，后续run view确认真正终态。该旧CI不覆盖8ad33ed或新pgbackup/CI改动。Goal仍active，正式审核待完整开发后统一进行。

此前推送checkpoint（已被上述状态更新）：组合已推送 `ed446fb`，当时CI34319259511仍in_progress；全组织清单与真实pg_dump交由并行单元继续开发。其后CI全部成功、清单已完成并提交，准确证据见上方，原临时DB占用安排不再有效。

本轮已实际提交 `5503ff8`（同一只读事务完整审计链）和 `d9ed20d`（PostgreSQL导出快照生命周期）。上一轮用户请求进度核对，未计新增开发；本轮有真实实现、验证及提交。旧基线 `3ffad26` / CI34204020837已实际全部success，含quality、Windows/Linux package、image、全部repository/Worker race、identity PG、依赖及required；这不用于追认上述新增单元，新CI以实际推送后运行身份为准。

新审计helper仅复用指定实际sql.Tx，不另开读视图或取PG SHARE；原公开full/tail事务、错误/部分计数/末尾时间语义保留。真实502/505条分页、多组织、另连接提交不漂移、第二页第501条篡改/超长字段/取消/缺key/typednil/裸连接等均通过；SQLite物理RO仍须调用方在专有Conn.Raw验证，query_only不是替代证明。原功能和快照组合双库三轮53.693s PASS；49个名称含Audit的真实仓储顶层测试三轮222.644s PASS；Windows/Linux目标vet/lint0，Linux仅静态不冒充原生运行。详见M6-AUDIT-SNAPSHOT-NOTES.md。

PG内部helper实际RR/RO、事务局部有限timeout、服务端快照标识/时间、同一事务审计校验与另一事务导入已接通；实际UPDATE拒绝且吞错后Commit仍失败、消费错误保留、pool等连接取消、已观察PgSleep后的运行中取消、回调panic/rollback均测。独立代码自查未确认生产P1/P2，但提出服务端失效证据缺口，已补每个失败路径实际重新导入直到服务端明确42704；首轮测试误写22023导致7.027s失败，真实诊断2.167s及PG源码确认后修正，不是生产红测。补强最终五top三轮15.493s PASS、vet/lint0。不是pg_dump导出文件、全产品回归或恢复演练。详见M6-POSTGRES-SNAPSHOT-NOTES.md。

本地既有测试PG在恢复时无进程/无监听；只启动经state核对的原受管集群。30秒启动helper超时后实际server26484仍在WAL恢复，继续观察同一进程直到ready再测试，未重启活进程、initdb或删除锁/数据。首次PG未启动的审计测试1.297s失败如实保留；不是新产品灾备演练。两个专有agent工作已交回，当前DB由root管理。契约三轮最终0.787s PASS；此后只增PG测试与文档，下一CI仍需独立确认。

下一单元：实际pg_dump与同一快照完整inventory/报告规则引用、受控认证和子进程退出，再接文件/配置归档、恢复隔离与激活、CLI/HTTP/UI。M5遗留、M6其余交付、M7/M8及SEC/ALG/OPS仍保持原77需求/91任务完整范围；M6-05、SYS-008和Goal未完成，正式审核仍统一待全部开发完成。

### 2026-09-08 checkpoint（已被本轮状态更新，保留原证据）

组合已推送 `3ffad26`，新CI34204020837已实际确认in_progress，跟踪handle由derived_s1保留；尚无新CI通过结论。root当前全量 `go vet ./...`、`golangci-lint ./...` 均通过，webassets实际二进制已编译至 `.tools/mii-v1-integration-check.exe`（仅开发构建，不是RC制品）。下一单元已交evidence_capture：在调用者持有的同一只读事务内复用完整审计算法，新增SQLite/PG快照稳定性证据，尚未交付。它独占本地DB，其他agent不得并发DB；root负责最终集成与Git。原77需求/91任务不变。

本轮实际完成并提交：`f62109a`测试循环修正、`ebd81b3`安全失败诊断（与此前`8093012`时钟修正已推送）；`14e9919`审计数据库侧字节界限、`74d20a1`真实SQLite在线快照、`1980e59`Docker完整测试包串行、`0746431`退出会话标题测试时序、`e4d0d31`Windows夹具祖先/私有child区分。新组合的远端CI尚待验证，提交/运行身份以实际Git/GitHub为准，不用旧CI追认。M0-04两项修复2238e68保留，不重做。

真实SQLite在线快照已修正只读CHECK漏验（先真红0.263s），DONE后目的RW连接固定有界完整性验证，关闭后仍独立只读复验；source仍只读。真实WAL/>24MiB、多步/容量/取消/忙锁/FK/损坏副本、PRAGMA参数/遮蔽专项三轮5.012s PASS，38状态机故障纯层三轮0.092s，Windows/Linux目标vet/lint0，Linux仅交叉编译。原同包集成测试循环已通过外部测试包+test-only fixture bridge修正；新Docker使用 `go test -p 1 ./...` 协调默认64MiB共享scratch，仅串行包二进制，保留全包/全部用例/内部并发/原期限与race门禁；契约真红0.090s→完整三轮1.099s PASS/lint0。镜像原生运行待新CI。

审计字节界限真实超大字段红绿、精确canonical上限/合法空摘要MAC重放和operational/worker回归已通过。当前组合完整repository双库单轮 **548.761s PASS**（含自然lease expiry，不排除任何测试）、CI相关Worker三父项双库三轮 **70.435s PASS**、完整Worker双库 **176.908s PASS**。完整API默认SQLite **51.110s**/显式PG **96.826s**，app两次 **59.061s/58.968s** 均PASS，app自身涵盖双库。不是完整全产品终审或恢复演练。

CI34202061195 / ebd81b3已实际终态failure：Linux package/image/dependency/identity PG/全部repository与Worker race成功；Windows16项staging夹具错误与quality前端1/911失败，required正确失败。新闭集日志定因profile唯一others traverse(0x20)被误作private拒绝，前两层/root/TI均通过；修复只按原ancestor策略pin/复验profile，新随机child仍严格private，不改生产或真实ACL。实际合成同权限反例红0.086s→绿0.090s，完整privatefile三轮 **3.334s PASS**、vet/lint0。该CI Windows Worker **172.057s PASS**，更早辅助SQL/claim错误本轮未复现，原生根因不假称已修。

前端失败精确为App.test.tsx:571账户全退后标题断言，测试仅用现有waitFor等待App effect更新精确标题，并先确认原账户/强制改密标题；全部会话树/密码canary/API次数断言保留。修后定向8项2.71s、完整34文件 **911项32.96s PASS**，typecheck/lint/build及webassets二进制编译通过；JS533.23kB原警告未抑制。新远端复验仍待执行。正式审核统一待全部开发完成；原77需求/91任务和所有剩余M5/M6/M7/M8/横向范围不变。

### 前一轮有效状态（保留当时记录）

本轮继续实施：上一轮按用户要求核对进度，不计新的开发交付；此前 CI34196169387 / `fb223e8` 已实际核验全部 success。本轮已提交 `54a9c31`（专用 SQLite staging），未用旧CI追认本轮改动。maintenance与新app测试正形成独立提交；当前远端/CI以实际 Git 和 GitHub 为准。

维护门禁已由root完整复核。最终限定repository单轮413.096s PASS（仅排除另有最终双库120.865s证据的自然lease expiry用例）；旧完整Worker166.751s失败已独立诊断：原 captured-only 注入双库30轮出现一次真实 unavailable_seal/COMPLETED/recorded，未命中约束。仅将display表INSERT改为无条件真实故障，要求明确ErrConflict，保留所有原子回滚断言，并在移除唯一故障后用同一completion/原lease成功结算、raw/display各1行、HTTP仍仅1次。修后双库30轮25.833s、原四类故障及独立captured正向三轮14.518s、完整Worker双库单轮155.026s PASS，vet/lint0。未改变生产密封策略；unavailable_seal底层原因仍待诊断。

root新增真实app正常/隔离初始化两次重启对照，首次7.868s为测试错误解读helper返回值（不是生产红测）；修正实际HTTP有界envelope读取后，双库三轮10.050s通过、app lint0。隔离状态503且12类表计数不变、normal首轮201/重启409，持久门禁不变。此合成隔离中间态没有真实restore入口，不冒充恢复演练。

`54a9c31` SQLite staging：实际journal代际红0.115s→green0.112s；新Windows实际SQLite/native十八项0.493s、root完整privatefile三轮3.359s、contracts三轮0.585s通过；Windows/Linux lint0，Linux只crossbuild。root已完整读新生产/测试并复核严格路径/权限/sidecar/失败清理。下一真正SQLite online backup helper由evidence_capture实施，只允许专有只读source、零busy timeout、有限Step和唯一finalizer、只读完整性校验；当前DB时段已交该agent。尚无新helper运行成功或系统内备份/恢复结论。

完整范围仍为原77需求/91任务，M5遗留、M6其它交付、M7/M8与横向专项均未缩减。Goal active，正式审核待开发完成后统一进行。

## 本轮早期 checkpoint（保留当时状态）

最新核验：已提交/推送 HEAD 为 `fb223e8`（生产流式清单校验）；新CI34196169387已创建，root watch session88154实际poll仍live，未取得终态。上一 `b7e9a90` / CI34192651533 已实际全部 success，但不包含新stream、migration21门禁或SQLite staging。上一轮按用户要求只答复进度，本轮已有实际实现、反例修复、验证与提交；不重做 M0-04 或既有单元。

维护门禁：session41303最终快速/历史三迁移双库三轮70.710s、真实Worker维护三轮31.382s、真实60秒自然lease expiry双库单轮120.865s通过。新增Initialize实际红测0.588s证明schema21隔离空库仍创建组织/用户/关联/设置/审计；原事务首部3行business admission修复后，新初始化+精准历史seed语义回归双库三轮18.553s、vet/lint0。root已完整复核全部新门禁生产/测试和新历史helper，未提交。

扩大回归session71394已终态exit1：整个repository单轮413.096s PASS（唯一排除 `TestSystemMaintenanceNaturalLeaseExpiryFencesLateRenewalAndTakeover`，其此前最终单轮120.865s证据单列）；完整Worker单轮166.751s FAIL，唯一 `TestRunWorkerDisplayActualTLSPreparedThenLostLeaseOrSQLFailureNeverPersists/display_insert/postgres` 返回了成功。原故障SQL仅限制captured，测试未先确认实际captured，故目前不能称生产绕过约束或唯一归因门禁。derived_s1仅在失败分支补SQL CASE闭集状态/存在性诊断，原注入和失败断言保留；compile-only0.094s，无复现结论。需与staging交接DB时段再复现，不能追认全Worker通过。

本轮已交付 `fb223e8`：生产VerifyStream自己有界校验准确entry集合/长度/hash/真实EOF，未知项不读取、吞错/panic/取消仍失败、返回后能力失效。新实现复合EOF先红0.136s→精确EOF修复；完整包三轮0.312s/93.2%、lint0，最终contracts/manifest三轮0.878s/0.291s通过。真实25MiB+17 synthetic payload校验累计分配<4MiB；实际合法AEAD与不一致清单、全entry正确但归档尾截断/追加内容均有真实反例。不是数据库快照或恢复入口。

SQLite staging：7个新生产/测试文件已落盘并仅compile0.090s，Windows native-only十项两轮/lint0；Linux仅crossbuild，不算运行。root已读shared/Linux/Windows生产与shared/Linux tests。已指出合法DELETE journal删除重建会与终身inode绑定冲突；首轮0.105s仅因测试os.SameFile身份夹具失真失败，不冒充生产红测，专有agent正在固定真实原生identity后验证/修复。当前由evidence_capture占用自建SQLite时段，终态后交给derived_s1原Worker诊断；无共享PG并发迁移。只在系统卷根owner对已精确验证TrustedInstaller作窄OS可信例外；宽D:\根/Temp仍拒绝，不修改系统ACL。真实双库快照/inventory、恢复隔离/激活、CLI/UI、M5遗留与M6/M7/M8/横向完整范围仍未完成，Goal保持active。

## 维护门禁最终回归之前 checkpoint（保留当时状态）

当前已提交并推送至 `b7e9a90`，包含 `b1f98f0`（canonical manifest / 归档清单 / 预解码资源限制 / 测试 / 需求映射）、`ab509c3`（私有文件与认证归档真实组合/fuzz）、`ba68eab` crypto 和 `6044d27` 历史主密钥加载。CI34190326051 / dc26d9a 与 CI34191395103 / ab509c3 均已终态 success，全部质量/打包/镜像/双库race/依赖及required门禁通过；watch12686、19485均exit0关闭。当前新CI34192651533 / b7e9a90实际运行，root watch session4350已实际再poll确认live；最近尚有quality/repository-race-0/Windows包未完成，其他job成功。不能用前两轮CI追认新manifest或未提交门禁。

本轮完成并提交 `ab509c3`：25MiB+17真实privatefile/crypto嵌套、no-replace、9种异常及完整认证后真实mtime改变导致源原生复核失败且不发布；最终Windows全privatefile三轮1.848s、root新mtime三轮0.104s、两包lint0，原10秒fuzz159084次/11.130s PASS。新manifest结构/资源反例修复后，新包三轮0.165s/92.7%和lint0；最终contracts/manifest三轮0.551s/0.159s通过，10秒parser fuzz 266次/11.106s PASS。独立反例证明约57MB先分配缺陷修至约1.6KB前置拒绝，合法模板128字符不再误用key64上限。详见 M6-BACKUP-MANIFEST-NOTES.md；这些均不等于数据库快照或恢复验收。

维护门禁migration21仍未提交，DB回归由专有agent独占。已修singleton↔认证event关联（先红1.287s）及普通业务admission直接回置初始normal的绕过（先红0.613s）；修后维护专项双库8.326s通过，normal保留最新operation指针，未提供snapshot/restore activation许可。真实60秒lease自然到期/审计迟到/接管/旧generation双库单轮120.743s通过。新受控锁等待发现SQLite既有BEGIN IMMEDIATE不及时响应200ms ctx、越过3秒（PG实际wait edge取消通过）；固定driver busy callback已定位，正在仅对维护协调器引入独占连接的临时busy_timeout/恢复及失败废弃，不修改公共5000策略/不放宽2s期限。上述门禁最终故障回归与新helper尚未收口，不能用早期通过掩盖真实失败。

下一步仍是维护门禁最终验证、真实SQLite/PG一致快照与inventory、文件/配置归档、恢复隔离和全链校验、CLI/HTTP/UI。原77需求/91任务、M5遗留、M6其它交付项、M7/M8及横向专项仍保留完整范围；本Goal未完成。

门禁最新在途证据：agent session35654顺序运行，快速维护双库三轮50.257s和真实Worker三轮20.728s已PASS，旧仓储兼容单轮仍在途，之后才到新helper的真实60秒lease双库单轮。专属SQLite连接helper使受控200ms锁等待约0.31s退出，恢复/物理废弃及Store池身份测试已通过；公开Begin的cleanup失败保持已commit freeze而返回失败，不冒称回滚。lint发现按值copy gorm.Statement含sync.Map及3个选择器提示，须待在途测试终态后修正再验；当前不是lint通过。root完整复核现20个旧文件接线与Worker/迁移/锁/连接测试，另指出新gate audit SELECT整列大小未受限，agent将在终态后补字节受限读取与真实反例。旧audit.go的initial anchor/full/tail同类读取边界留待快照/审计单元，尚未修复，不隐藏。

下一SQLite实现的只读调研已确认：modernc1.58.0 NewBackup会按URI重开目标；Windows现createTemp共享0不兼容，需要专用guard/工作目录和journal继承ACL；Linux默认VFS会解析/proc/self/fd为实际路径，不能声称经dirfd固定写。拟新增整条可信祖先链检查及专用私有staging，禁止跨用户路径替换，仍排除可信sameUID/root攻击者。root已直接读Backup.Step/Commit/Finish/NewBackup与Tx代码：只读Tx选项不等于mode=ro/query_only；Finish吞目标Close错误，成功应明确Step DONE→单次Commit→checked Close，失败清理限制仍需处理。设计未实施，没有真实数据库快照证据。

## 本轮之前 checkpoint（保留当时状态）

最新收口：响应清理 `1ef1573`、CI分组 `2ca76f3` 均已推送；CI34189727828终态failure，实际全部repository/Worker共12个race分片、显式PG identity/API及依赖扫描成功。quality/image/两种打包均因遗留netns Go契约仍要求旧Other/needs失败；root已真实复现0.094s并修正精确新分组/门禁及负例，完整contracts三轮0.547s、组合lint0。不能称新CI已绿，也不把该契约失败当作实际netns隔离失败。

备份并行进展：纯分块认证crypto完成本地专项待root最终提交；root新增真实多文件历史主密钥加载与app配置/启动接线，旧/新审计SQLite/PG三轮4.584s通过，纯keyfile0.435s/配置0.268s三轮通过。维护门禁migration21由专有agent实施并独占DB回归，尚未交付。上述仍不是备份/恢复或M6完成。

当前响应清理单元以 `c7e5ae3`（Windows 辅助 SQLite 连接与闭集诊断回归）为本地前置提交，已推送 HEAD 仍为 `6fdfd6c`；migration20 / repository / worker / app / API / UI 已完成本地集成验证，提交身份以 Git 记录为准。原77需求/91任务与全部开发后统一审核目标不变。上一轮是只读进度答复，期间取得在途 SQLite/PG API 新通过结果；本轮继续实际集成与提交，不将状态答复计作开发交付。

响应清理已实际提交 `1ef1573`（50文件），未包含备份纯crypto或CI改动。随后CI并行单元完成：quality保留全部Core44包、Worker六片和显式PG identity/API独立任务、原repository六片不变；所有race/10分钟进程/25分钟job/required外部名称保留。root完整复核改动并复跑87离线分片、130策略反例、actionlint通过；agent真实非race Go RE2枚举repo241/Worker62全部恰好覆盖。原生race/新CI仍待推送验证，非race枚举或离线合成日志不算真实测试体运行。详见M5-CI-RACE-PARALLEL-NOTES.md。

修正后的真实0/30天SQLite/PG应用清理链路，root session81561最终PASS32.471s：实际HTTP政策0→30、每日维护/Job/物理副本删除、删除凭证/授权正文null、全部S1/分析与JSON/HTML报告字节哈希不变、两权限撤销403、全审计。随后全 webassets/app 双库三轮 PASS150.603s（web0.396s），覆盖独立多块正文授权场景。新CI、浏览器与其余保留类别未完成，不能据此关闭整个M6-07。

真实应用清理测试曾在session24634 双库30.161s失败于PG retained计数断言；root随后单PG session48589 7.302s同样失败，并证明实际18 Attempts中17条captured、1条unavailable_seal，API retained17完全匹配数据库。BodyRecorded并不表示展示封装必成功。root已修正helper为独立raw/captured副本数和S1-only不可用记录不变验证，正向正文测试选真实captured样本；未改生产封装失败策略、密文时间或安全阈值。修正后真实应用验证已通过，不把该次unavailable_seal的唯一原因猜成时钟偏差。

独立复核确认并修正清理三项P2：删除事务末尾consumer复验、数据库/审计同源时间、失败首Run阻塞同组织后续Run。专有agent真实红绿与扩大回归已通过；另新增disabled真实用例揭出通用Claim/reconcile会取消清理Job，已加入严格完整batch绑定的最小例外，普通Job/legacy/cancel/超限仍拒绝。summary存量元数据关联/时间反例及真实不可用S1保留测试已补齐。正式审核未批准，M6-07未关闭。

前端root已完整复核新API/Panel及其测试，5文件183项4.13s通过；最终全web34文件911项34.77s、typecheck/lint/build通过（session69438，exit0，JS533.23kB警告保留）。OpenAPI及12字段纯契约完成；root新契约/DTO/API/handler三轮0.215s/0.110s/0.099s/0.099s通过。以上不替代实际清理闭环或浏览器验收。

最新CI34187516642已终态failure：quality在25m4s被取消，停在race步骤；Windows失败在第二轮webassets的真实多块正文撤销evidence.read辅助SQL，其他9项成功，required gate正确失败。quality真实分区进展/日志正在独立诊断，不把取消猜为某具体测试失败。裸SQLite测试辅助连接busy_timeout=0/fk=0已由受控实际writer锁反例证明会立即SQLITE_BUSY，辅助连接已对齐生产每连接配置并补闭集错误分类；root四项临时SQLite/安全诊断三轮0.410s、完整contracts三轮0.500s通过。原CI缺driver code，不能声称Windows唯一根因已证实。原期限、race覆盖、required gate不变。

仓储清理最终双库三轮143.240s、清理与原Job/Queue/Audit单轮49.225s、旧展示/保留政策/迁移39.987s均已终态PASS；root新API/run/app/worker/contracts lint0、完整contracts三轮0.500s。root session83572已终态exit0：全webassets app三轮150.603s、完整API SQLite50.177s/PG91.782s、完整Worker双库145.402s均通过。这不是完整仓储包/全产品最终回归或CI证明。备份纯分块认证流已有9项专项首轮0.257s通过，额外反例与完整回归仍在进行；不混入本次清理交付，也不计作数据库备份/恢复完成。

## 上一已提交单元记录（以下为当时快照）

最新实际提交已推送 `d52aee6`（正文service/API/app/前端、17项服务与多块真实TLS场景、安全诊断）及 `6fdfd6c`（保持正则语义的脱敏扫描必要条件优化）。后者本机同900KiB准备约4.5倍改善，root evidencedisplay/secret全包三轮1.157s/0.465s、lint0；原2s期限/输入/regex不变。新CI34187516642针对6fdfd6c实际运行，尚无全绿；d52的34187262046被后续推送取消，不能计成功。

当前下一单元是真实响应正文清理：migration20、每日持久调度、精确批次Job/删除凭证/审计签名校验、worker/app、S1只读状态API与报告区面板均已落盘，仍未提交。仓储初轮双库及扩大故障回归已通过，root发现调度early return缺末尾consumer复验、typed错误被通用层吞没后补修与专有反例正在收口；root实际应用清理前后S1/JSON/HTML制品不变helper已接，尚待DB验证。前端891项/typecheck/lint/build通过，不替代真实清理闭环；OpenAPI/契约同步中。此单元不包含完整请求复现、样本180天、聚合/报告365天、审计密封分段清理，整个M6-07不关闭。

上一轮为用户请求的只读进度答复，不计开发交付。本轮继续实际实施。已推送至 `01165e1`：`3601a65` 旧模式正文捕获遵守最终组织留存策略；`6031891` 独立 request-only 脱敏/专用加密纯组件；`83abaa2` 七处执行查询将取消/驱动故障误标失租的修复；`01165e1` 完整 Worker race 六组顺序执行。响应正文仓储/migration19 已本地提交 `22c9046`；服务/API/app/前端最终集成与追加诊断仍在收口。原 77 需求 / 91 任务范围及统一审核目标不变。

CI：`01165e1` / `34185808446` 已终态failure。Linux打包、镜像、六组repository race、依赖扫描成功；Windows第二轮webassets的0天SQLite在第二个Run状态轮询失败，13.46s尚未达到15s周期续租，已补安全分类诊断但根因未证实。quality在前置非repository/非Worker race组 `TestRequestReproductionLargeBoundedRequestAndExpansion` 返回MI_DISPLAY_CANCELLED（2.73s）；实际Worker六分组尚未开始，不得把离线分组stub日志当执行证明。不能混同旧limited/sqlite renew故障或旧600s累计超时。未减少测试/放宽race与期限/修改required gate；当前仍无全绿结论。

正文读取：repository/service/API/app 已实际接线。Source/Disclosure共享生命周期修复后，仓储/执行查询双库三轮73.345s、实际0/30天TLS→发布→受限正文HTTP三轮75.585s通过；后加17项真实服务场景单轮30.875s。新增实际TLS大正文多块测试双库13.924s通过：首64KiB后撤权、取消、permit/原会话自然到期和管理PATCH0停止后续块；首轮测试错误按随机ID排序已改真实审计sequence，不改生产。完整API按SQLite50.232s/PG83.307s串行通过。最终包含诊断、17项helper和大正文的webassets全app双库三轮session26993终态PASS138.674s（web0.500s），root诊断纯三轮0.114s及app/run/API/contracts组合lint0。该快照不含后续清理migration20或正则优化。

请求复现纯组件已复核，evidencedisplay/secret三轮1.034s/0.525s、lint0；尚无请求策略/存储/Worker/HTTP/UI，REP-005不关闭。正文前端实际显式入口与切换Attempt清理已完成，完整816测试/typecheck/lint/build通过，root专项87测试与契约三轮通过；Vite >500kB警告未抑制。浏览器工具拒绝新测试URL，未取得正文页面验收；该次未带webassets的hold仅是后端证据，不得误算前端。完整Worker双库回归session67620终态183.003s PASS。详见 M5-DISPLAY-READ-INTEGRATION-CHECKPOINT.md；实际到期物理删除及可验证凭证仍未实现。

## 历史 checkpoint（保留原始当时状态，不作当前结论）

最新测试终态：旧正文保留单元仓储/Worker专项/API/app均通过，准备独立提交；全Worker第二轮146.047s只有circuit/model/PG清理JOB_LEASE_LOST失败，既有原文断言已修正并通过。停机SQL错误分类及独立own-WithLease续租阻塞正在单独红测，不误称已修复前次WindowsCI。正文service/API/app测试代码仍是自有pending等待仓储接口，不计运行完成。

续轮checkpoint：上一轮为用户请求的只读进度核对，不计开发交付。旧模式正文capture仓储最终双库三轮145.782s通过；root实际策略/取消/轮换/熔断三轮31.893s、结果HTTP SQLite4.683s/PG8.354s、全app双库27.416s/contracts0.236s通过，Worker/API lint0。首次全Worker140.420s仅三组旧raw存在断言失败，已改成真实上游未发送正文后的not_captured/双表无行/正确stop分类与完整审计，原时限/计费/次数不放宽；全Worker收尾仍在跑。最新远端52557c3 CI34183736103 Windows在limited/sqlite的renew DATABASE_UNAVAILABLE失败，根因未定；不是新legacy断言问题，更不是该Windows任务中的PG迁移竞争。quality仍运行，整体未绿。响应正文授权读取和独立request-only可信复现纯层已开始实际实施，尚未交付，REP-003/005/M6-07不关闭。

最新集成已提交推送52557c3：新签名模式、实际Worker/S1结算、分析读取/认证/发布、维护恢复和app默认入口已接通；CI34183736103运行中，尚未全绿。前次两组混合失败涉及的取消/0天完整TLS及读侧所有真实反例，待其它测试进程终止后root串行双库三轮66.103s全部通过；不因此追认此前DATABASE_UNAVAILABLE的唯一根因。仓储扩大三轮557.914s通过（在预算一行修复前已编译，预算修复由另外43.890s/79.410s证明）。现在继续旧已确认legacy的0天写入：私有capture/最终策略接口与Worker接线正在实施，新实际legacy0/7/30/180及0/30全18样本分析双库首轮6.308s通过；原Manifest/ConfigSnapshot不变、0天无raw/display INSERT且无伪S1，结果明确INSUFFICIENT/D。授权正文/复现、清理仍未交付。

本轮提交与真实失败记录：4771690 已提交物理行 S1 绑定；9405e03 已提交 migration18、正常/重新认领/终态维护仓储接线。真实在途 deadline 双库红测发现最终预算分类漏计未知输出的保守费用，已只增既有收费 switch 的 MI_EXECUTION_BUDGET_EXCEEDED 分支；root 专项三轮43.890s、实际失租+预算两场景三轮79.410s通过。读取侧派生仓储三轮309.998s通过，既有全部 ^TestAnalysis 双包单轮repository219.545s/worker187.123s通过，lint0。混合并行验证不得冒称全绿：root Worker组合三轮204.668s在RunCancel/PG一次claim DATABASE_UNAVAILABLE失败；另混合三轮299.510s在0天实际TLS/PG执行一次DATABASE_UNAVAILABLE失败。已观察测试迁移advisory排队，但Claim不获取该锁且缺当次driver code，唯一根因尚未确认；不放宽生产2秒期限、不忽略失败。待当前已在途扩大回归结束后，由root统一串行复测。应用三轮79.678s通过仍是独立真实证据，不覆盖此失败。

应用派生接线最新结果：0/30天完整应用各真实SQLite/PostgreSQL三轮79.678s通过。策略经真实管理HTTP设置，0天使用DB约束禁止任何raw/display INSERT，仍完成18样本分析、复核/基线、JSON/HTML制品、再次9样本检测、幂等回执和审计；两Run全部新模式，每个实际Attempt一条S1，0天正文均零，30天对应存在。入口已实际改为新模式（当前尚未提交），维护不再pending。详见 M5-DERIVED-APPLICATION-NOTES.md。旧legacy正文写入/授权读取/清理和UI仍未完成，不能关闭完整0天需求。独立Worker只读复核未发现确认P1/P2，指定双库五组回归8.954s；真实derived在途失租/预算中断正在补专有证据，不以既有legacy测试替代。

恢复与集成 checkpoint：上一轮仅只读进度答复，不计开发完成。b6d03ec / CI34182141504 已实际全部成功，包含 quality、Windows/Linux 打包、镜像、六个 repository race 分片、依赖扫描；quality 的 Offline replay OS network isolation 也已成功。仅证明该已提交快照，不包含当前 migration18/派生执行与应用接线 WIP。终态维护适配已是实际 run_reconcile_derived.go，真实双库三轮4.691s通过；不存在仍等待 .pending 接口的阻塞。root 本轮已接 app 新签名模式、用途隔离 S1 capabilities、正常执行/分析/维护，原实际初始化→TLS→发布/报告链路双库首轮12.745s通过；通过管理 HTTP 设置0天并强制数据库拒绝两类正文 INSERT 的完整应用回归正在执行，尚未作为完成证据。物理 SQL 行与 opaque S1 MAC 绑定薄校验完整复核、专项三轮0.483s及 features/app lint0；该薄校验不能代替完整 BuildDerived。

实际派生链路 WIP checkpoint：root 已接新签名模式的 Start、真实 Credentials.Use 内策略/DB 时钟捕获、实际响应四候选、CompleteWith 按最终结果选一条 S1、重新认领 UNCERTAIN 与分析发布。新双库测试三轮27.213s：0/7/30/180天、捕获后禁用/启用/单调cutoff、0与30天各完整18样本发布；0天在raw/display实际数据库禁止INSERT下成功，30天独立读取真实密文精确AAD解密后的 BuildResponseReference 与完整已发布 analyzer JSON 逐字相同。分析handler不持EvidenceKeys也可完成。最终取消/stale/deadline/多重覆盖及S1/审计/失租回滚三轮23.404s；重新认领旧真实Attempt（目标已stale、配置无凭证/解密能力）三轮2.566s，只留一条认证UNCERTAIN、无重复HTTP、原代次不改、预留归零。root全Worker119.662s/app15.871s/contracts0.265s通过（Worker/app实际双库；为当次编译快照，不替代后续新增修改测试）；相关lint0。

此链路尚未提交，依赖正在完成的migration18/仓储读取/终态维护。app默认来源模式仍未切换。Worker维护适配暂存于自有 run_reconcile_derived.go.pending，等待真实仓储API落盘再编译和测试，不算交付。旧已确认legacy Manifest不重签，当前旧Worker正文仍固定30天，必须继续改为遵守最新组织策略；legacy在正文不可用时明确证据不足，不能伪造S1。0天整体需求还包括旧模式写入、授权读取、清理、展示入口，不能因新模式测试通过提前关闭。原77需求/91任务与统一审核目标不变。

签名源模式集成 checkpoint：Manifest Options 新增可省略的 analysis_source_version，旧 canonical/hash/MAC/golden 不变；新派生模式禁止 Build(raw) 或缺失 S1 自动降级，显式 BuildResponseReference 必须验证全部 S1 与每个实际 raw 来源（含非最终重试）。run.Service 只接受可信启动配置，HTTP 不允许选择来源模式；切换配置后未确认旧草稿失效，旧确认回执/Manifest 不重签。root 完整复核新生产/反例，generator/features/replay 三轮通过（0.850s/23.814s；core1.509s、capture16.759s、CLI12.881s、localfile0.811s），新真实 HTTP 控制面三轮 SQLite1.296s / PG2.421s。app 尚未启用新模式，migration18/执行器/分析读取仍在接线；不以纯层或控制面通过宣称0天完整生效。

管理/保留 P2 集成 checkpoint：root 完整复核两项生产修复与全部专有测试。匿名审计外键与组织策略锁的实际 PostgreSQL 死锁改为兼容外键 KEY SHARE 的 NO KEY UPDATE，不增加重试；管理事务成功 callback 后复验原会话自然到期，跨期时业务/审计同回滚，合法主动自撤销仍可提交。root 两项组合三轮33.120s通过（管理真实SQLite/PG；死锁只针对PG），agent真实红绿及扩大回归见 M5-RETENTION-CHECKPOINT.md / M6-MANAGEMENT-EXPIRY-NOTES.md。这是实际生产边界修复，不是阶段批准或完整保留策略验收。

恢复 checkpoint（覆盖下方旧 CI 待验证描述）：上一轮是只读进度答复，不计开发完成。本轮继续实际装配。f03a320 / CI34181012515 的 Linux 打包、镜像、六个 repository race 分片、依赖扫描和 quality 内 Offline replay OS network isolation 已实际成功；quality 当次查询仍运行，Windows 打包失败，整体未绿。Windows privatefile 的短 TMP/TEMP 基路径已真实本地复现同症状：普通写立即 ErrUnsafe。仅规范测试自建 TempDir，双句柄确认同一目录；生产别名拒绝、NTFS/ACL 策略均不变。root 完整复核新测试代码，全 privatefile Windows 三轮0.942s/81.3%、lint0通过，每轮确实分配8.3别名；下一远端 CI 才能证明该 Windows 失败已消除。此时 migration18/Worker 正在并行开发，不将组件通过当全仓通过。

组织保留策略 checkpoint：migration17 只新增单调 cutoff；实际管理设置锁后取 DB 时间，days/cutoff/version/审计同事务，CAS 必须一行；0→30/7→30 不复活既往正文。新私有 Policy 提供非写准备快照与事务最终组织锁，禁止 JSON 传输；双库触发器拒绝 cutoff 回退。root 完整复核生产/迁移/所有新测试，专有双库三轮16.514s、实际初始化→TLS→发布证据双库两轮24.392s通过；agent 管理/迁移扩大双库三轮94.602s，identity/API SQLite三轮21.508s/14.736s、PG36.935s/29.166s、最终专有双库17.280s及lint0。旧16→17失败DDL/历史账本/旧设置完整回滚恢复已测。Worker 仍固定30天，未接最终策略/派生源/正文读取清理，不宣称0天生效；下一实际装配见 M5-DERIVED-WORKER-INTEGRATION-NOTES.md。

netns 修复 checkpoint：实际 IPv6 CONNECT EADDRNOTAVAIL 只在连接前后同一独立空源拓扑证明下接受；通用 errno 判据仍拒绝。新增唯一 lo DOWN/接口地址空/IPv6 地址表空/原无可用路由复验，CLI 完成后再复验；缺文件/坏表/读关失败/任意地址/namespace 漂移均失败。补 native 同步 no-source、close 失败、关闭时取消/恰好一次 close、零 receipt/异步/权限/成功反例。固定三个 proc 路径，不关闭 gosec 规则。root Windows CLI/契约三轮10.011s/0.437s，PolicyOnly、Linux tagged 交叉编译、lint0通过；独立只读复核无确认 P1/P2 放行漏洞。仍需新 Ubuntu 实际执行，不是 OS 验证已完成。

最新 CI 已终止：e8a52a6 / 34179919964 为 failure；Windows/Linux 打包、六个 repository race 分片、依赖扫描成功，image 在 refusal 完整分析 JSON 等价断言失败，quality 在隔离 IPv6 CONNECT 返回 EADDRNOTAVAIL 后失败。不能沿用旧局部或全绿基线。评分已实际定位：原 refusal 1000 轮复现 3 次 RepeatabilityFactor 末位差异，真实冻结计划的 3/3/1 簇 raw-only 重复分析也失败，证实无序 map 浮点累加不稳定。仅固定 family 累加顺序，公式/精度/阈值/golden 不变；修后原场景 1000 轮 230.900s、五分析包三轮与 lint0，通过。新增 raw128 次字节稳定及受限差异诊断反例。netns 另行以实际空源拓扑组合证明修订，尚待新 Ubuntu CI。

备份文件底座checkpoint：新增生产privatefile，root完整复核生产代码；独立复核修复Windows目录FILE_DELETE_CHILD漏检P2，真实Everyone仅0x40红测→绿测，拒绝读写且原文件不变。root Windows三轮0.868s、lint0；agent三轮0.919s/81.3%。64KiB流式/32MiB+17真实数据、8并发仅一成品、发布后失败Published=true、只清理自建staging、路径/ACL边界已测。Linux仅交叉编译/lint/源码审查，实际执行待CI；容器overlay拒绝不放宽，测试使用验证为tmpfs的独占目录且不skip。SYS-008/M6-05仅底层组件进行中，维护门禁/快照/归档/恢复/CLI/UI仍缺。

可信派生S1纯层checkpoint：同一实际提取器生成有界typed观测、独立derived-s1-authentication用途MAC、完整Manifest/请求/最终Attempt复验后恢复原分析器；behavior聚合/配对仍共用唯一实现，不持久化nonce/seed/原句或任意Kernel Input。18类完整分析JSON逐字节等价及真实Secret purpose/历史版本轮换通过；root features/behavior/secret三轮15.899s/0.428s/0.600s，secret独立HKDF九purpose golden与八既有用途不变、限定能力并发/诊断保护通过。生产secret与仓储禁止反向导入分析运行时的AST契约扩展；无0天Worker/数据库/报告交付声明。根任务继续装配真实结算和保留策略，详见M5-DERIVED-S1-NOTES.md。

正文采集Phase2a集成checkpoint：实际Credentials.Use内使用真实Key及全部自定义Header脱敏并独立封装，短WithLease绑定DB时间，migration16与既有Attempt/raw/Job/audit同事务结算；不改原分析Content/tokenizer/Usage/metadata。root已复核新生产/SQL/故障测试。agent新展示套件双库三轮repository40.708s/worker34.803s、完整Worker双库114.548s通过；root实际app初始化→受控TLS→发布证据双库三轮36.368s、contracts三轮0.292s通过。迁移失败DDL/历史账本回滚、采集后失租及真实SQL/末尾ctx取消无孤立记录。仍固定30天，无正文HTTP/UI/复现下载或0天生效声明；详见M5-EVIDENCE-CAPTURE-CHECKPOINT.md。system期限修复4c73c63已推送，另PG系统HTTP三轮11.465s和相关lint0。

最新CI诊断（覆盖此前运行中状态）：dfd0e56 / CI34179042934 的 quality、Linux打包、镜像均在 `TestSystemStatusHTTPAdmissionAndAuthenticationDeadline` 同一断言失败；此轮未运行到后续netns验收。原断言以调用ServeHTTP之前的时间作为内部WithTimeout的精确起点上界，Linux的更精细时钟使两次取时差被误判。修正仅比较真实SetWriteDeadline观测到的取时区间，仍严格验证两秒期限，增加更短父期限必须原样继承；不改生产期限、测试开关或CI门禁。Windows SQLite该专项10轮3.942s通过。最新源码还有正文采集/派生S1/备份底层并行改动，不以此专项当全量通过。

最新浏览器补充（覆盖下文此前“待收尾”状态）：使用computer-use在新建本机SQLite/受控TLS测试实例，完成账号安全两步确认全会话撤销→登录回执→刷新仍未登录→原密码重新登录→系统页真实读取→账号页取消确认→普通退出。系统/账号页面390×844视口document clientWidth/scrollWidth均375，截图核对未见整页横向溢出，console warn/error为空；恢复视口并关闭本次测试标签，不操作用户已有实例。测试进程handle在Goal续轮后不存在，62594无监听，不能据此宣称该hold进程最终测试返回值通过；浏览器操作本身已有直接证据，普通非hold后端测试单列。没有多设备浏览器或完整M7故障验收声明。

系统/会话完整接线已提交fa191d8；root恢复后contracts三轮0.352s、netns包装策略通过。B1-5真实导出已全文复核：独立ManifestSigner、实际发布/结算与审计/decoded canary通过后逐文件私有原子导出，关闭TLS后的真实CLI预测与原发布字节相同，换key精确ErrIntegrity、已有输出精确ErrExists。agent实际双库rollback十轮67.557s，全replay/contracts三轮capture122.608s/core1.724s/CLI11.536s/localfile0.877s/contracts0.750s，额外两TLS及guard三轮60.598s；双平台lint0、Linux交叉编译通过。强制completion回滚的测试改为live ctx下等待ErrUnavailable，正常stop仍严格nil，不改变生产。B1-4数字socket/500ms/poll+SO_ERROR保持明确不可达闭集，新增family/stage/errno脱敏诊断，Windows策略/契约/交叉编译只能作为本机证据，实际Ubuntu结果仍需CI。真实源CLI与synthetic netns为两项独立证据，不冒称真实源已在禁网环境验收或M5-06已完成。

最新远程基线：da6db58 的 CI34111776546 已结束，Windows/Linux打包、镜像、依赖扫描和六个repository race分片通过；quality在新增Offline replay OS network isolation失败，required失败。旧日志只证明身份、拓扑及loopback负控制通过，不能断定egress失败唯一根因。后续数字socket和闭集errno诊断修订须另跑Ubuntu CI；不跳过检查，不以本地交叉编译冒充OS隔离证据。

本机集成checkpoint：实际 `go test ./... -count=1` 全部通过，repository349.785s/worker176.778s/app39.165s为真实SQLite+PG，identity23.374s/API80.823s为SQLite；回放capturefixture45.551s。固定工具链，测试耗时不是性能容量声明。系统状态与退出所有会话接线后前端30文件/744测试33.27s、lint、typecheck/build通过；入口515.47kB的体积警告保留。首次定向130测试中的旧标题断言曾在DOM已提交而effect未完成时失败，改为等待原精确标题、不改产品行为，最终全套通过。新系统/会话HTTP与契约已补齐；OpenAPI56路径/75操作/116schema（包括计划接口，不是完成数），52实际DTO漂移检查及相关lint0。

SYS-006只读状态checkpoint：同组织系统角色+run.read+audit.read授权快照、末尾2事件/有界Job/真实本地Runner、二次授权复验、认证前2秒及2/1并发保护已接实际app；未实现的远端观测、配额、保留清理、备份明确unavailable，主密钥/报告仅startup_verified。root系统四包三轮repository21.171s/identity0.786s/API5.995s/app7.383s通过，repo/app实际双库，另有PG身份/HTTP专项。实际SQLite浏览器已验证登录→系统→读取→手动刷新；本次未完成窄屏/退出所有会话浏览器收尾，旧hold句柄恢复时已不存在，不虚构该次hold最终测试结果。页面不是整体系统健康或完整M6交付证明。

展示Phase1已提交74c7240，root两包三轮0.500s/0.476s、覆盖率86.8%/81.3%及相关lint0；独立display purpose、有限凭证脱敏、认证绑定/到期/取消和私有载荷已实现，既有七purpose golden保持。Phase2 Worker/数据库/可信派生S1/0天保留和Phase3授权HTTP仍待完成，不关闭REP-003/005。上一状态答复仅作只读进度核对，不计开发完成；恢复后继续集成提交与实现。

SYS-002自助全会话撤销checkpoint：服务/仓储已提交eac402c。可信会话取得User/SessionID，用户→会话锁后复验有效期/禁用/改密，不要求组织或管理员；全会话撤销与多组织审计同事务，真实审计INSERT失败回滚，拒绝旧authority再次撤销后续新登录，不改密码。root仓储双库三轮21.615s、identity SQLite三轮4.620s、显式PG identity/HTTP三轮12.258s/7.627s、HTTP SQLite三轮2.552s通过；只读agent复核无确认P1/P2。HTTP覆盖Origin/CSRF、闭集空对象、退出Cookie、强改密和无组织；账号页和强改密页两步确认已接线，503/未知回执明确结果未确认、不自动重试，退出/切换清空并忽略迟到响应。相关前端124测试12.10s及上述全套通过，真实浏览器仍待收尾。不把事先取消当持锁期间到期或严格2秒墙钟证明，不声称正式批准。

最新checkpoint：c9f8fa6的CI34109951576已失败：六个repository race分片成功，但quality、Windows/Linux打包、依赖扫描及镜像因测试导入循环失败，不能作为全绿基线。循环为tokenrisk测试→secret→repository→scoring→tokenrisk；修复将总览revision-1兼容版本保留为独立常量，用跨层测试绑定真实冻结版本，并增加生产仓储不得反向导入分析引擎的AST契约，不删除原测试。root tokenrisk与contracts三轮0.770s/0.251s、Overview双库三轮33.529s及相关lint0通过；`go test ./... -run '^$'`只证明全包测试编译通过，不是全量测试执行。新修复仍需远程CI。0266102离线CLI、79afdec真实Worker捕获/计时修复及cf63a89进度同步已本地提交。原77需求/91任务完整范围及待统一正式审核不变。

B1-2真实捕获/计时checkpoint：实际初始化、会话、目标、TLS预检、估算/确认、Worker/精确AAD→未提交禁止封存→真实最终结算/发布→关闭TLS服务→离线重放已在SQLite/PG验证；publication后故障完整回滚、无sealed bytes，decoded Manifest/wire/body/header检查控制端合成口令/Key。nonce序列两行仍强制真实token计数≤64最小tier。PG真实锁等待旧版红测Adapter359ms/DB30ms→seal拒绝；修复只去掉错误跨起点包含关系，保留两者180s、已验证Manifest请求/Run预算及TTFB/TTFT/events内部时序，生产和既有结果未变。agent双库五轮41.179s；root ./tests/replay/...三轮core1.085s/capturefixture24.854s/CLI10.777s/localfile0.579s和lint0通过，原发布分析与重放逐字节相同。捕获仍仅内存，文件导出/独立CLI真实源接线和OS禁网继续，未完成校准或规则发布。

B1-4禁网checkpoint：新增Linux专用tag测试，既有quality步骤强制实际CLI在非root/清空cap/独立netns/lo DOWN/无可用路由下运行，宿主loopback前后正控制、隔离内负控制、真实S1逐字相同、负例和原子文件测试。包装器要求精确run/pass、无skip及8阶段各1次，PolicyOnly不能作为OS证据；原required和测试不变，Docker仅补测试源不进scratch。root Windows contracts/CLI/localfile三轮0.278s/8.759s/0.464s及PolicyOnly/actionlint通过；agent Linux交叉编译/lint0、策略16反例/原CI107回归通过。已将Start后cancel的调度竞态改为启动前取消且Process为nil，真正中途取消由afterSync屏障另证，不冒称graceful信号。Linux实际OS结果仍待本提交后CI，真实捕获导出仍未接通。

B1-3 CLI checkpoint：tests/replay/cmd/replay真实离线可执行文件、localfile私有普通文件/独立开发Manifest signer/规范信任文档及原子不覆盖输出已实现。root Windows localfile/CLI三轮0.392s/8.608s、lint0；新增无需symlink权限的真实junction读写拒绝后localfile三轮0.392s及lint0。agent覆盖率localfile83.6%/CLI80.2%，Linux交叉编译/lint0，尚未实际Linux运行或OS级禁网演练。CLI只是合成源组件测试；真实Worker捕获导出/完整离线演练仍未完成，不导出生产master、不冒称发布准入。

组织总览checkpoint（c9f8fa6，已推送）：GET /overview同授权快照验证run.read与target.read，组织IANA日历7/30日、当前目标、Run状态、冻结版本风险分层及可空整数费用真实聚合；migration15只加当前目标部分索引。后端agent双库仓储11.086s/API PG3.165s，root实际app双库三轮61.209s、contracts三轮0.235s及相关lint0；前端27文件623测试及typecheck/lint/build通过，root总览专项105测试再通过。真实SQLite浏览器登录→7/30切换→刷新→历史→返回→退出通过，当前目标1/Run2/两个包分层/费用未知；无console错误，正常宽屏截图核对通过。hold121.660s包含交互等待，不是性能证据；没有新增窄屏/活动撤权浏览器验收。

Windows锁反例第二checkpoint：observer/blocker显式保留两个物理连接，短可取消gate覆盖真实持锁区间，严格blocker更新1行、observer真实SQLite5/PG55P03拒绝后回滚，不屏蔽job Renew、不重试、不改生产或期限；所有准备步骤只记录闭集phase/driver code/时间/Ready。agent双库定向十轮14.353s、全worker168.670s、lint0；root定向三轮3.828s通过。原CI缺driver code，唯一根因仍未知；远程Windows结果必须另行核实。

B1-1集成证据：`tests/replay`严格规范JSON/开发签名/私有验证源、真实离线HTTP/SSE解析到Tokenizer/features/A1 Runtime已实现；root三轮0.747s、golangci-lint0，agent三轮0.776s、覆盖率91.0%，先前decoder fuzz10秒678137执行通过。嵌套S2容器fmt/slog/JSON保护已有回归。当前仅合成组件案例；源发布hash仍只是开发控制端声明，不证明真实DB提交。实际Worker/TLS捕获、独立CLI禁网演练、验收集与发布准入未完成。

Windows租约fixture checkpoint：只改两个测试文件，不改生产。独立连接在短可取消gate内真实更新、严格1行和新owner/generation读回，HTTP原2秒从更新提交开始；mock四秒逃生不能冒充真实取消。独立写锁反例保留，SQLite BUSY(5)/PG55P03不等于失租。agent定向双库十轮15.287s、取消/故障/维护组合三轮58.485s、全Worker双库80.516s及lint0通过；root定向双库三轮4.527s通过。gate不排除job Renew竞争，原CI唯一根因仍未知，新推送CI结果需独立记录。

AC-17错误正文checkpoint：新增真实TLS→Worker派发/结算→精确AAD解密回归，400/401/HTTP200错误对象/畸形JSON/SSE错误事件/合法前缀后SSE错误六场景，SQLite/PG三轮14.836s、worker lint0。错误体中的实际合成Key未进入解密后的分析证据、Attempt元数据或完整Worker日志；合法SSE前缀保留、失败分类/计数不被伪造为成功。该证据不证明恶意响应头或成功正文已脱敏；独立展示purpose、0天派生S1/保留cutoff/最终读取审计仍在设计与分阶段实施，不能据此关闭REP-003/005。

A1集成证据（f1987a3）：受限开发规则artifact/resolver和实际参数注入7包三轮通过（bundle0.757s、analyzer0.546s、features2.227s、scoring0.820s、tokenrisk1.024s、generator0.693s、datasets0.440s），分析相关golangci-lint0；真实SQLite/PG `worker -run TestAnalysisWorker -count=3` 9.907s通过。参数变化实际改变结果、旧完整JSON golden保持，候选不启用A/B或发布。B1捕获方案已完整核对并开始测试工具实现，不增加生产证据读取旁路。

趋势后端集成：`GET /runs/trends`真实数据库聚合/快照/权限/筛选/签名分页、正常/重试/未知/在途/缺失延迟全部有定向测试；三轮repo22.978s/run0.099s/SQLiteAPI9.838s，PG新HTTP三轮8.173s通过，相关lint0。主任务实际应用Quick18+Custom9 TLS检测到趋势、排除预检、原修订/分母/真实延迟/分页三轮双库35.911s通过。新增契约后全contracts三轮0.203s通过；主任务复跑新趋势/契约repo4.060s/API1.913s/run0.109s/contracts0.177s通过。前端及浏览器收口继续。

CI5d30e24/34105436577：6个repository-race分片、Linux打包、依赖扫描和镜像构建/标签/SBOM/扫描均已实际成功；quality与Windows当时仍运行，不能宣称整体已绿。新A1及趋势提交仍需各自后续CI。

最新恢复补充：报告页面、两Run比较、最终发布撤权故障隔离及镜像构建源码修复已提交a9fa108。之前7f376a9本地最终全Go双库回归通过，但CI 34102058547失败，不能作为绿色基线：Windows/Ubuntu打包和依赖扫描成功，镜像缺web源码、quality仓储包race累计10分钟超时（非DATA RACE报警）。完整race分片修复已完成：仓储6个独立PostgreSQL矩阵任务，严格枚举Test/Example/Fuzz并各运行恰好一次，其他包与额外PG identity/API回归保留；每包原10分钟上限不变，required检查必须等待所有分片成功。107项策略回归、44项分片自测、actionlint通过；本机缺CGO编译器，真实race性能及镜像结果待新CI确认。A1纯核runtime与趋势API已冻结待集成；趋势前端和契约继续开发。较早a7eefff的34097436068为Windows取消失败；0565e83/34096744592及3f8404e/34094006283取消，不计通过。更早09afbe8/34094651688、a22f113/34092338705、50e8353/34091448697全绿只能证明各自提交。

M5 当前接续：`7f376a9` 已接入人工复核API/页面、组织基线API/页面、真实报告Job/私有文件/下载；应用受控TLS完整后端双库及全Go通过。后续报告页面已接线，452前端测试/build/lint及真实浏览器生成和JSON/HTML验证下载通过。可信基线评分、规则发布、重分析、完整对比/正文及其余M6～M8仍未完成。评分开发/未校准且最高C，无有效证据D；四项成对BH不是独立盲测/FDR验收。统一审核未进行。

版本包 checkpoint（`3f8404e`）：应用启动验证冻结规则/模板/tokenizer 哈希，注册真实分析处理器；初始化/新组织原子 seed，旧组织有界幂等补登记，同版本字节不同拒绝、不覆盖/不退役恢复。开发包无正式发布时间/批准/校准指标。估算和确认事务核对实际包内容；旧草稿版本失效拒绝新执行，原确认回执仍幂等恢复。该次全量 Go 测试通过，repository91.814s/worker74.973s实际双库、API26.075s/identity13.393s SQLite；新的读取单元另有下列证据。

版本包收口证据：`go test ./internal/integrity/repository -run TestBootstrap -count=3` 实际双库三轮 14.572s，通过 101 组织跨分页补登记、初始化/新组织 seed 审计失败完整回滚、同版本篡改/已退役拒绝、无伪发布时间/回放指标。设置 `MII_IDENTITY_TEST_DRIVER=postgres` 后 API 全包 33.383s、identity 全包 11.907s 通过；新旧运行时草稿/永久回执回归已覆盖。全部使用隔离合成数据，无真实上游计费。

SSE checkpoint（`09afbe8`）：真实 HTTP 流/SQLite/PG 覆盖取消到终态、旧/同/未来游标重连、成员/会话/账号/密码撤销、DB故障、连接限制与释放、生命周期和无 S2。PG SSE23.122s、窄Version双库0.537s。客户端本次补充终态GET完成前不展示终态的回归，共26测试；RunProgress 已接线，增加组件回归。修复测试裸`1000000`匹配随机ID/hash的偶发误报，保留敏感容器/正文/Secret断言并新增合法ID回归。

## 流程与恢复入口

依据项目方本次 Goal 授权，逐项人工审核/Gate 签字统一推迟至完整候选版。前置实现与必要验证通过即可继续开发；原有 M0-01～03 批准保留。**开发完成不是正式审核通过**。原计划的审核标准、范围和量化发布线不变。原计划状态作为正式历史保留；本台账独立追踪开发。

恢复工作先读本表、git status/log 和需求矩阵，再读取下一模块规格。不得重做已验证单元或覆盖其他 agent 的工作。提交不得包含 .tools、真实凭证或测试数据库。main 保护不变，不以绕过独立审批推进合并。

原始审核基线：`028b66d`（PR #5，M0-04 待审核）；持续集成在草稿PR #6。原始需求 77 项（59 P0、17 P1、1 P2）；任务 91 项。本次Goal全部纳入，原PRD的future/P1/P2只保留历史分类，不能作为排除理由。

## 单项进度

实现状态：未开始 / 进行中 / 已实现待验证 / 已验证 / 外部待执行。正式审核：保持既有批准或待统一审核；不能以测试通过代替签字。证据为空表示尚未获得。

需求矩阵维护：SYS-001～007已按实际代码、双库/页面测试和已记录CI回填为进行中，不再保留错误的“全部未开始”；各行明确完整会话管理、系统状态页、生命周期、Compose干净启动等缺口，SYS-008备份及未做事项不提前升级。CFG-003区分已实现内存真实捕获/合成源CLI与尚未接通的真实文件导出/OS禁网/验收集。原77项、优先级、91任务归属和待统一审核不变；contracts三轮0.241s验证路径/覆盖，不代替功能验收。

| 任务 | 实现状态 | 提交/制品 | 测试命令与结果 | 缺陷/阻塞 | 正式审核 | 下一步 |
|---|---|---|---|---|---|---|
| M0-01 | 已验证 | 既有审核记录 | 见 docs/reviews 原始证据 | — | 已通过（既有批准） | 保留基线 |
| M0-02 | 已验证 | 既有审核记录 | 见 docs/reviews 原始证据 | — | 已通过（既有批准） | 保留基线 |
| M0-03 | 已验证 | 既有审核记录 | 见 docs/reviews 原始证据 | — | 已通过（既有批准） | 保留基线 |
| M0-04 | 已验证（集成CI） | 2238e68、032c347、df14c09 | 95项policy回归；df14c09 CI34084712215质量/双库race/Windows/Linux打包/镜像/依赖全部成功 | 新5b84e45及之后提交须独立CI | 待统一审核 | 最终RC全量重跑 |
| M0-05 | 已实现待验证 | docs/design/m0-05-prototype.html | go test ./tests/contracts：13 页导航/覆盖检查通过 | 视觉与实际前端 E2E 待实现 | 待统一审核 | 真实前端接入后视觉验证 |
| M0-06 | 进行中（实际读取和制品API已验证） | docs/api/openapi-v1.json；tests/contracts/system_status_test.go、sessions_test.go | 56路径/75操作/116schema（含计划接口，非完成数）；52实际DTO漂移检查；contracts三轮0.309s及相关lint0 | 规则发布及其余尚未实现的契约需继续接入 | 待统一审核 | 契约与产品继续同步 |
| M0-07 | 已验证（测试框架） | 6ee0796 | go test ./tests/mock-upstream ./tests/datasets ./cmd/mock-upstream；lint 通过 | 公开开发示例不是完整/盲验收数据集 | 待统一审核 | 用于协议/算法测试，补隔离验收语料 |
| M0-08 | 已验证（追踪框架） | docs/project/requirements-v1.json | go test ./tests/contracts：77/77 需求归属覆盖通过 | 功能代码/测试证据尚待逐项填入 | 待统一审核 | 随实现补充真实代码与测试映射 |
| M1-01 | 已验证（持久化底座） | 035a0b6 | 真实 SQLite/PostgreSQL18.6：迁移/回滚/校验和/并发/租户约束全部通过 | 应用启动接入另由 M1-02/09 完成 | 待统一审核 | 新结构变更必须新增 migration，不修改 000001 |
| M1-02 | 进行中（开发包初始化已验证） | 8295fd3、7a85cb5；bundle/builtin.go、repository/bootstrap_bundles.go | 双库原子 seed/审计回滚/幂等与同版本字节校验；真实应用初始化重启保留包记录 | 正式校准/完整初始化浏览器验收仍待完成 | 待统一审核 | 完整端到端初始化验收 |
| M1-03 | 进行中（身份后端已验证） | 8295fd3、e1041ad、cb41628 | 双库：Argon2id、审计、Cookie、Origin/CSRF、IP 限速、临时密码强制修改、重置/禁用撤销会话；浏览器登录/退出通过 | 完整管理页面 E2E 待验证 | 待统一审核 | 管理端权限 E2E |
| M1-04 | 进行中（服务及HTTP已验证） | e1041ad；api/identity_management.go | 用户/组织/成员、5角色、最后管理员/CAS/审计回滚：服务与HTTP真实双库通过 | 管理页面正在实现 | 待统一审核 | 真实管理页面联调 |
| M1-05 | 进行中（管理页面已验证） | catalog服务/API、daeafbb | 双库CRUD/CAS/权限/引用保护；目录前端新增30测试，全前端181测试/build/lint通过 | 新目录页面真实Go浏览器联调尚待执行 | 待统一审核 | 真实浏览器及完整E2E |
| M1-06 | 已验证（队列核心及原子终止修复） | 0fa6a68、c3c757b | 双库队列核心、独立进程排他、SKIP LOCKED及fence；正常Claim的终止UPDATE0真红→修复，完整Job/Precheck双库三轮39.835s | 完整维护drain仍按M6推进；新修复尚待本批远端CI | 待统一审核 | 完整备份协调及最终容量验收 |
| M1-07 | 进行中（追加/验证及原子写入已验证） | 035a0b6、c3a092c | 真实event INSERT0/head UPDATE0八场景红→修复；新旧审计/20并发/故障双库三轮32.652s，维护组合40.337s、并发初始化2.242s；lint/vet通过 | 保留密封、DB授权与完整安全验收尚未完成；新修复尚待远端CI | 待统一审核 | 分段生命周期与完整运维验证 |
| M1-08 | 进行中（Run前端checkpoint已验证） | 07648da、9b743ae、0fe9428、9994a88 | 151前端测试通过，35 Run专项复测/build/lint通过；旧管理目标真实浏览器通过 | 新Run页面尚待真实Go浏览器；目录管理正在补齐 | 待统一审核 | 完整端到端联通 |
| M1-09 | 进行中（真实Worker就绪已接入） | 7a85cb5；internal/app/app.go | DB/schema/审计尾链；Run首次消费者心跳后ready，停机/失租变false；停止后SQLite租约可重取 | 完整执行器/监控指标尚待开发 | 待统一审核 | M2执行器与M6指标 |
| M2-01 | 进行中（服务/HTTP已验证） | 3a6eb4a、4ff7726；api/targets.go | 双库服务：目标/凭证原子事务、CAS/8路竞争、租户、活动Run删除拒绝；全API SQLite7.48s/PostgreSQL15.30s含HTTP生命周期/权限/分页/严格JSON通过 | 页面浏览器整合待完成 | 待统一审核 | 真实目标管理浏览器联调 |
| M2-02 | 进行中（加密/文件/持久化已验证） | 630dda7；internal/integrity/secret | 信封/AAD/轮换/fuzz850785次；Windows ACL/junction/hardlink；双库密文与零回显测试通过 | 出站Worker/报告/备份全链路泄漏扫描待完成 | 待统一审核 | 版本化Secret服务与Worker调用整合 |
| M2-03 | 已验证（Worker TLS集成） | 8ad5358、5b84e45 | SafeHTTP 94.2%覆盖+fuzz92417；真实受控TLS双库验证压缩/限长/超时/取消 | 真实付费兼容性不在开发测试内 | 待统一审核 | 全链路安全回归 |
| M2-04 | 已验证（Worker集成） | d14ac4d、5b84e45 | 非流协议/参数/失败分类/受控TLS双库测试通过；Run禁透明参数回退 | 完整检测到报告尚待M5 | 待统一审核 | 端到端受控上游 |
| M2-05 | 已验证（Worker集成） | d14ac4d、5b84e45、09afbe8 | 流/非流/partial/malformed/压缩与重试；真实HTTP SSE、撤权/连接上限及前端接线验证 | 活动Run浏览器SSE故障注入仍待整体E2E | 待统一审核 | 完整端到端回归 |
| M2-06 | 进行中（执行/API/页面测试已验证） | 1211b11、5094ac7、10897f8、7edf2dd、0fe9428、aedb4f0 | 双库受控TLS预检；权限撤销事务复验；Worker全包双库五轮84.694s；df14c09 CI全绿 | 新页面受控上游浏览器E2E待执行 | 待统一审核 | 完整受控上游E2E |
| M2-07 | 进行中（运行时到可读结果已验证） | 5b84e45、4b30aed、a22f113；app/pipeline_test.go | 实际应用初始化→TLS预检→确认→Worker→分析→历史/结果/S1读取，SQLite/PG通过 | 报告和后续基线/规则/复核未完成 | 待统一审核 | 接报告生成和下载 |
| M2-08 | 进行中（租约/取消/熔断已验证） | 5b84e45、ad0cb99；migration10 | 取消/轮换/失租<5s、UNCERTAIN不重复；2/2/5连续失败熔断双库；1000样本停止1.676s | Runner当前单作业串行；实际多作业并行/容量待M7；2/2/5为开发策略 | 待统一审核 | 运行时维护与并发容量 |
| M2-09 | 进行中（金额与确认漏洞已修复验证） | 5b84e45；本次控制层/预算checkpoint | nil金额强制known-price硬上限；新API双库覆盖zero/未知/改价/撤权；仓储三轮16.069s | 尚需完整检测链路到报告的预算验收 | 待统一审核 | M5整合及端到端硬停止 |
| M2-10 | 进行中（进度与历史已验证） | 9994a88、09afbe8；web/src/components/runs | 报价/确认/取消/SSE/终态GET组件验证；真实浏览器读取历史/结果通过 | 浏览器完整配置→确认→运行故障流程仍需补；报告未实现 | 待统一审核 | 完整浏览器执行E2E |
| M3-01 | 进行中（模板与签名生成器已验证） | 879e998、f47a282、235db5b | 20开发模板、规范SHA、Manifest HMAC、96bitnonce、变量/随机顺序/重放与日志脱敏测试 | 持久化包发布/独立批准私有池尚缺 | 待统一审核 | 版本注册/发布与分析接线 |
| M3-02 | 进行中（生成器核心已验证） | f47a282、235db5b | Quick18/Standard60/Deep120，持续序列/JSONL有限任务上限、严格body重放通过 | 尚需实际Run到证据展示 | 待统一审核 | 接M5特征与执行链路 |
| M3-03 | 进行中（阶梯编译已验证） | f47a282、b12f133 | 整组预算降级/重复与成对数据保留；标准60真实编译合成输出回归通过 | 生成器开发上限150样本；完整校准未完成 | 待统一审核 | 统一特征和真实数据回放 |
| M3-04 | 已验证（离线Tokenizer内核） | 43073e8、df14c09 | 官方tiktoken0.14.0离线759语料×2词表=1518计数一致；资源上限/56805fuzz/lint；许可证随包交付 | 输入聊天framing仅compatible/heuristic，不冒称计费精确 | 待统一审核 | 运行时与证据Token质量展示 |
| M3-05 | 已验证（结构特征内核） | 4df5e0f | JSON/JSONL/序列/Markdown/结束一致性与资源边界测试，81026fuzz | 只输出特征，不等同风险结论 | 待统一审核 | M5统一有效性和下钻 |
| M3-06 | 进行中（聚合内核已验证） | b12f133 | 97.4%覆盖/三轮/vet/lint/12128fuzz；固定256在标准60检出两族平台各80，加权Token41；健康0 | 41不是>=60验收通过；独立盲测/FDR/置信校准尚缺 | 待统一审核 | M5校准与可信基线 |
| M3-07 | 进行中（Usage/终止特征已验证） | 43073e8、4df5e0f、b12f133 | 至少6同方向Usage、缺失/推理/启发式降级、最终重试去重、阶梯适用分母测试通过 | 不输出正式证据等级 | 待统一审核 | M5统一聚合 |
| M3-08 | 进行中（对照与指纹内核已验证） | 631cef1、b12f133 | paired stream CI、仅stream截短不误作统一平台、后缀缺失/未检查区别测试通过 | 实际证据流到页面尚缺 | 待统一审核 | 统一特征构建 |
| M3-09 | 进行中（真实Token页已验证） | web/src/components/results；api/run_results.go | 真实TLS样本展示：全量档位中位数/MAD/CV、当前页散点、Usage/流式分母、S1样本/Attempt | 正文/复现模板和可信基线比较尚缺 | 待统一审核 | 补授权正文与复现入口 |
| M4-01 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-02 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-03 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-04 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-05 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-06 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-07 | 进行中（模板/纯特征内核已验证） | 879e998、f47a282、631cef1 | 格式/中性白名单/差分/风格/多语言/指纹/辅助自报与严格配对测试通过 | 开发白名单不代表独立批准；M5聚合和实际页面未完 | 待统一审核 | 统一特征与完整检测回放 |
| M4-08 | 进行中（行为特征和配对统计已验证） | 631cef1 | 92.4%覆盖/五轮/vet/lint/45417fuzz；严格最终Attempt与配对、差异与重复特征 | 未实现M5最终四维风险/等级与独立校准 | 待统一审核 | M5评分/置信/基线 |
| M4-09 | 进行中（真实行为页已验证） | web/src/components/results；run/result.go | 实际浏览器：重复指纹、四项探索性配对BH、替代解释、零权重自述声明；空/权限组件测试 | 基线差异/正文对比和更细结论仍缺 | 待统一审核 | 与基线/证据深化整合 |
| M5-01 | 进行中（分析/读取闭环已验证） | c1ebe76、50e8353、a22f113、3f8404e；run/result.go | 特征89.0%覆盖；TLS签名/AAD/最终Attempt双库到持久结果和真实HTTP读取；冻结版本校验 | 报告与历史版本重分析仍缺 | 待统一审核 | 报告和固定版本重分析 |
| M5-02 | 进行中（开发评分已验证） | c1ebe76；analyzer/analyze.go | 四维权重、缺失重归一、风格/单族上限、Token41保持；scoring三轮98.0%覆盖 | 未完成独立校准及可信基线 | 待统一审核 | 规则注册/校准证据 |
| M5-03 | 进行中（开发置信/分级已验证） | c1ebe76；scoring/analyze.go | 五因子、partial<=59/未校准<=74、禁止A/B、零有效D、四项成对BH | 正式A/B需可信网关/校准准入，尚未实现 | 待统一审核 | 可信证据及校准边界 |
| M5-04 | 进行中（S1聚合发现下钻已验证） | repository/analysis_result.go；run/result.go；ResultsPage.tsx | 原子发布+权限快照读取+闭集S1，真实浏览器到Finding引用与Attempt | 更细规则证据、正文和复现模板仍缺 | 待统一审核 | 授权证据和报告 |
| M5-05 | 进行中（组织参考API/页面已验证） | baseline/service.go；repository/baseline_*；web/src/components/baselines；migration12 | 来源Manifest验证、独立MAC、审批CAS/过期/退休/撤权双库三轮；真实浏览器创建/审批/退休/回读通过 | 可信对照采样/评分准入未实现 | 待统一审核 | 接可信比较及后续准入 |
| M5-06 | 进行中（真实捕获/独立CLI/OS禁网已验证） | bundle/artifact.go、runtime.go；tests/replay；b6d03ec | 实际Worker双库结算/发布→私有文件导出→关闭TLS后CLI逐字重放；CI34182141504实际Ubuntu Offline replay OS network isolation通过 | 独立校准、持久化版本准入/发布/退役未完成；新提交仍需各自CI | 待统一审核 | 独立校准与规则生命周期 |
| M5-07 | 已验证（追加复核API/页面） | repository/reviews*；api/reviews*；ReviewPanel；migration13 | 双库并发CAS/原收据恢复/撤权/审计回滚；实际浏览器追加/刷新/HTML纯文本且机器结果不变；日志/孤立Unicode反例修复后通过 | 报告人工复核快照尚未实现，不影响原复核读取 | 待统一审核 | 完整产品端到端及报告联动 |
| M5-08 | 进行中（JSON/HTML/CSV产品链已验证） | 2357652、f2d5cde、6507c02、5269782；M5-CSV-INTEGRATION-NOTES.md | 真实app双库30日/0日三轮111.340s；浏览器288.417s；前端930测试；新观测版双库两pipeline三轮115.035s | CI34430463313的Windows报告GET 5s deadline未本地复现，真实原因待新CI观测；复核快照、PDF、完整版本差异/清理配额未完成 | 待统一审核 | 按真实等待层修复CI问题，继续其余导出与生命周期 |
| M5-09 | 进行中（历史/比较/单目标趋势已验证） | dfe3814后端；RunHistory、ComparisonPage、TrendsPage | 548前端测试；真实双库TLS/趋势三轮35.911s；浏览器比较/趋势筛选/空页/再读/下钻通过 | 完整差异报告/重分析/归因未完成 | 待统一审核 | 完整差异报告与固定规则重分析 |
| M6-01 | 进行中（新增账号安全与只读系统状态已接线） | web/src/components/system、SessionSecurity；internal/integrity/repository/system_status.go | 744前端测试及typecheck/lint/build通过；系统真实app双库三轮7.383s，SQLite浏览器读取/刷新通过；全会话双库/HTTP/页面测试通过 | 账号新入口完整浏览器收尾、规则/正文/维护等原P0仍未完成；状态上限不是容量验收 | 待统一审核 | 完成授权正文、规则与完整管理能力 |
| M6-02 | 未开始 | — | 未执行 | — | 待统一审核 | 构建单机一体化发布包 |
| M6-03 | 未开始 | — | 未执行 | — | 待统一审核 | 构建 Docker Compose 标准包 |
| M6-04 | 未开始 | — | 未执行 | — | 待统一审核 | 实现升级检查和迁移流程 |
| M6-05 | 进行中（归档完成链、候选暂停及共享恢复核心已验证） | 33f13e9、0385061、2b81f9f、26ee5ab、625574d、9757f8f、c90f18e及既有快照/载体单元 | 实际双库：执行全组3轮275.069s、引用/库存3轮184.972s、暂停fast3轮67.170s及自然60s各库1轮120.951s；新准备7.774s/非执行95.544s三轮通过；独立625检出TLS/MAC双库6.025s；旧ce8CI20绿不覆盖本批 | 完整遗留任务协调、全部资源依赖、密钥真实性校验、真实完整capture/恢复、CLI/API/UI及演练未完成；普通queue旧终止UPDATE0另修；合成库存不替代产品备份 | 待统一审核 | 接真实维护Load/Apply及完整资源收集，再集成快照/恢复协调器 |
| M6-06 | 未开始 | — | 未执行 | — | 待统一审核 | 实现指标、日志、Trace 和告警 |
| M6-07 | 进行中（响应正文读取/清理完整应用已验证） | migration11/17/18/19/20，22c9046、d52aee6；本次响应清理集成单元 | 全webassets/app双库三轮150.603s；仓储清理三轮143.240s；API SQLite50.177s/PG91.782s、Worker双库145.402s；S1/报告字节不变 | 浏览器及新CI待完成；请求快照、180天摘要、365天结果/报告与审计密封分段生命周期仍缺 | 待统一审核 | 验证新CI，再完成其余保留类别 |
| M6-08 | 未开始 | — | 未执行 | — | 待统一审核 | 完成制品完整性校验、版本和 SBOM |
| M6-09 | 未开始 | — | 未执行 | — | 待统一审核 | 编写用户、管理员和运维文档 |
| M7-01 | 未开始 | — | 未执行 | — | 待统一审核 | 执行需求功能验收 |
| M7-02 | 未开始 | — | 未执行 | — | 待统一审核 | 执行冻结盲测和算法校准 |
| M7-03 | 未开始 | — | 未执行 | — | 待统一审核 | 执行安全专项测试 |
| M7-04 | 未开始 | — | 未执行 | — | 待统一审核 | 执行性能和容量测试 |
| M7-05 | 未开始 | — | 未执行 | — | 待统一审核 | 执行故障注入和恢复测试 |
| M7-06 | 未开始 | — | 未执行 | — | 待统一审核 | 执行升级、回滚和灾备演练 |
| M7-07 | 未开始 | — | 未执行 | — | 待统一审核 | 执行报告文案和合规审核 |
| M7-08 | 未开始 | — | 未执行 | — | 待统一审核 | 生成 Release Candidate |
| M8-01 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 完成生产环境预检 |
| M8-02 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 部署灰度环境和 Smoke Test |
| M8-03 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 执行受控目标灰度 |
| M8-04 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 验证生产监控、预算和熔断 |
| M8-05 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 执行上线前回滚演练 |
| M8-06 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | Go/No-Go 评审 |
| M8-07 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 正式发布 V1.0 |
| M8-08 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 完成发布后观察与验收关闭 |
| SEC-01 | 未开始 | — | 未执行 | — | 待统一审核 | 建立威胁模型和数据分级 |
| SEC-02 | 进行中（核心安全边界已验证） | identity/secret/safehttp/repository | 真实双库RBAC/TOCTOU、ACL、AESGCM/AAD/审计、SSRF/TLS测试 | 仍需全链路泄露扫描/安全验收 | 待统一审核 | 完整产品安全集 |
| SEC-03 | 进行中（S2核心已验证） | f47a282、235db5b、5b84e45；草稿TTL工作单元 | HMAC/AAD/加密原子事务/日志脱敏/过期删除及回执验证通过 | 报告、备份、响应证据保留全链路仍缺 | 待统一审核 | 全链路泄露与生命周期检查 |
| SEC-04 | 未开始 | — | 未执行 | — | 待统一审核 | 完成渗透与供应链检查 |
| ALG-01 | 进行中 | 6ee0796；tests/datasets | 类型边界、内容哈希、跨分区/标签泄漏防护测试通过 | 独立标签持有人/完整冻结集尚未建立 | 待统一审核 | 扩充分层语料与独立盲验收进程 |
| ALG-02 | 进行中（公开开发回归集） | 43073e8、4df5e0f、631cef1、b12f133 | 1518官方离线计数、结构/行为/Token聚合及受控60计划回归通过 | 不替代独立标签持有人和盲验收 | 待统一审核 | 完整隔离语料/算法校准 |
| ALG-03 | 未开始 | — | 未执行 | — | 待统一审核 | 冻结评分和置信度版本 |
| ALG-04 | 未开始 | — | 未执行 | — | 待统一审核 | 完成盲测 |
| OPS-01 | 未开始 | — | 未执行 | — | 待统一审核 | 数据库容量、索引和归档设计 |
| OPS-02 | 未开始 | — | 未执行 | — | 待统一审核 | 监控、告警和 Runbook |
| OPS-03 | 未开始 | — | 未执行 | — | 待统一审核 | 备份、恢复、升级、回滚 |
| OPS-04 | 未开始 | — | 未执行 | 真实生产操作待授权；开发准备仍需实现 | 待统一审核 | 上线值守和容量观察 |

## 已验证工作单元

- 起点核验：工作树干净；有效 HEAD；现有分支 docs/m0-04-final-review；无其他活跃 agent。已从该 HEAD 创建集成分支，未修改 main/PR #5。
- 2238e68：修复 M0-04 自匹配与回读两项 P2；95 项离线回归通过。远端 GET 验证通过，无 PUT/保护变更。
- M0-05/06/08：低保真 13 页面、48 API 路径/67 操作、77 项需求追踪已建立。go test ./tests/contracts 通过；仅契约/设计结构检查，不声称产品功能通过。

## 当前并行范围

- root：06c2cab Windows fixture、0dd8915管理/锁P2、b6d03ec签名mode已提交推送；负责真实Worker/capture/重新认领/维护/app装配、TLS与完整raw oracle验证、集成与CI。
- evidence_capture：migration18、派生S1记录/receipt、私有capture、正常及重新认领结算、终态Job两阶段维护恢复；新旧migration/SQL guard/双库测试，随后旧模式正文策略收口。
- derived_s1：纯层mode和reference已提交；候选helper与薄VerifyDerivedRecord已冻结待整合；继续新独立真实Runner在途取消两路径验证，不改root生产文件。
- backup_scope：privatefile修复已提交；负责analysis_source/analysis_result和Worker分析读侧，全Attempt S1/物理行MAC/SQL限额/发布前摘要与S1行锁、真实双库负例；不改root执行器。
- 迁移000001～000017已集成冻结；新结构只能新增000018及以后迁移。PR #6 仍为草稿，main/PR #5未合并、保护不变。
- PostgreSQL18.6隔离进程仍在忽略目录 .tools、loopback15432；DSN静默读入环境不打印。其他agent仍需双库，不提前停止。
- 最新实际应用浏览器测试使用临时合成组织和受控TLS目标，已退出会话、恢复视口并关闭自建标签。hold句柄后来不存在/端口未监听，不能据此推断该hold最终测试通过或完整清理结果；直接浏览器证据与非hold后端测试分别记录，旧 .tools/v1-smoke 数据未改动。
- 剩余关键开发：可信基线评分/规则发布/报告其余功能；授权正文和复现模板；完整差异/重分析；生命周期/运维/双部署；全算法隔离校准/性能/故障/恢复及全部原P1/P2。Runner仍单作业，未证明100并发上游容量；CI34107691203除Windows外已成功，后续提交需独立全绿证据。

## 读取闭环 checkpoint 验证

- root 全量 `go test ./... -count=1` 通过：app17.805s、repository128.888s、worker102.425s（本轮实际双库），API52.903s、identity18.792s（本轮SQLite）。最后读侧微调另跑 HTTP 三轮 SQLite4.148s/PG7.711s、仓储读取/历史双库三轮15.495s、纯结果三轮0.275s，lint/vet通过；root最后全仓lint0 issues。
- 独立自查修复：任意版本字符串投影、零有效伪健康、Usage启发式分母、SQLite写锁/PG混合快照、SQL文本读取前无字节限制、可变筛选分页锚点。结构检查不是数据库对象的密码学认证，具体边界见DEV-RUNTIME-V1.md。
- 前端历史/结果单元261测试及build/lint通过；随后共享fetch固定8MiB成功/64KiB错误、严格UTF8、45秒deadline、断开取消与401/403退会话，传输66测试/全量309测试/build/lint通过。安全单元不是后端权限替代品。
- `go test -tags webassets ./internal/app -run 'TestApplicationActualTLSFromInitializationThroughPublishedEvidence/sqlite' -count=1 -v -timeout=7m` 浏览器hold模式280.424s通过（包含人工浏览器等待，不是分析耗时）。初始化/目标/预检/检测由真实HTTP测试完成；浏览器实测登录→历史→总览/Token/行为/S1/Attempt→退出，无控制台错误或CSP警告。390×844窄屏总览document宽度与scrollWidth均375（扣除滚动条），未整页横向溢出。活动Run SSE浏览器故障注入尚未做。
- API 52路径/71操作/80schema；15实际Go DTO字段、required/nullable、闭集结构漂移检查。无真实付费调用、无人工审批记录、无生产操作。
- 浏览器发现成功带警告 Attempt 错显 MI_EXECUTION_ERROR：持久化成功码为空串，投影错误按非nil兜底。现 nil/空串映射 null，保留实际outcome闭集；协议/Usage警告按20个明确代码补齐并合并样本协议警告，未知文本仍不回显。新增实际HTTP warning fixture：SQLite三轮4.806s、PG三轮7.158s通过；最终真实应用全包双库6.175s、contracts0.099s及相关lint0，通过真实Worker成功Attempt无错误码断言。

## 复核、基线、报告 checkpoint 验证

- 复核仓储/HTTP：原收据先恢复再检查latest-ID；6个旧收据重试+4个新CAS并发只有1条新记录；8并发首次写仅1成功；run.read/review.write、session/user/member/org任一撤销不得靠旧收据绕过；同事务审计失败完整回滚，原机器JSON不变。root `go test ./internal/integrity/repository ./internal/integrity/run ./internal/integrity/api -run 'TestReview|TestStrictJSON' -count=2` 通过（仓储实际双库13.549s、纯run0.141s、HTTP SQLite6.243s）；HTTP切换PG三轮18.749s通过。
- 自查真实反例：内部ReviewRecord/Input默认fmt/slog会输出人类说明（未观察到实际生产日志调用）；孤立surrogate经Go JSON静默替换后返回201并保存。现在内部输入/记录和授权ReviewView诊断格式脱敏，后者保留正常HTTP JSON；请求hash使用私有显式结构。严格JSON扫描拒绝非法surrogate，合法代理对/字面替换字符不误拒，已有反例及正向回归通过；Unicode fuzz34576次通过。
- 复核前端347测试/build/lint通过。实际 `webassets` 应用SQLite浏览器hold轮次145.207s通过（包含浏览器等待，非执行耗时）：已有受控TLS结果→人工复核读取→追加含script字样的合成说明→刷新读取两条记录→退出；无脚本执行、控制台错误或CSP警告，机器21/27/C不变。会话退出/临时应用已清理，不是正式项目审批。
- 基线 service/secret 三轮0.100s/0.102s、全部secret0.458s；仓储真实双库三轮17.134s，HTTP SQLite三轮7.536s/PG三轮8.938s通过。包括读快照期间并发写、撤权后下一读拒绝、取消释放、MAC篡改、有效期、高风险业务说明及批准后不再修改。
- 报告 Worker 双库4.146s、仓储报告/历史双库12.318s、API真实PG及旧结果HTTP15.454s；最终metadata/recovery上限与frozen identity补强后仓储6.140s、Worker2.412s、run0.298s、Windows存储0.494s、纯报告0.677s通过。Windows实测ACL/junction/硬链边界；普通symlink权限不足明确skip。Linux存储仅交叉编译+lint0，实际运行留给新CI，不冒称已验证。
- root `go test ./internal/app -count=3` 实际SQLite/PG31.793s通过：真实初始化/TLS执行来源、追加复核、基线draft→patch→approve→拒绝已批修改→retire、JSON/HTML异步Job→精确状态→下载。文件长度/hash/header/CSP、重试字节一致、基线不升级可信评分、机器结果不变和审计链均断言；报告目录错误拒绝、关闭后释放Windows句柄也通过。
- 基线前端新增42测试；全前端18文件389测试24.81s、lint、tsc/Vite build通过（58模块）。真实应用 `webassets` SQLite浏览器hold227.826s通过，包含手工浏览器等待：从实际来源Run创建新草稿→组织审批→已批编辑禁用→退休→刷新列表并重读详情→源Run机器21/27/C不变→退出。审批人/审批时间保留、退休时间另记，无console错误/CSP警告；已清理临时应用/数据库。本轮没有宣称完整浏览器检测配置或新报告UI完成。
- Windows取消确定性反例：合法2.2秒SQLite结算占用单连接，背景consumer pulse的2秒等待超时导致Runner取消自身结算；修前真实FAIL、修后可取消协调gate令SQL脉冲deadline在获得本进程结算协调后才开始，结算原10秒总期限/真实DB2秒/60秒租约和前后fencing均不放宽。双库 heartbeat/maintenance占用、gate取消和失租、新旧取消/轮换/关闭/DB故障/维护五轮115.422s通过，旧取消/轮换SQLite100轮29.712s通过，lint0。原CI是否另有触发仍待新Windows CI，故障已增加仅闭集phase日志。
- root第一轮全仓Go通过：API324.598s/identity93.523s（PG），repository429.162s/worker335.828s/app24.788s（实际双库）。该编译快照包含并行诊断时的临时CPU压力fixture，不作为最终性能或新协调修复验收；压力fixture已删除，不交付。去除临时fixture后的最终 `go test ./... -count=1` 全仓通过：API208.748s/identity70.387s（PG），repository269.410s/worker194.897s/app23.754s（实际双库）；这些是测试耗时而非容量验收。root全仓golangci-lint0，新的真实artifact范围/样本总量断言 app10.734s通过。
- OpenAPI 53路径/72操作/97schema，33个真实Go投影的字段/required/nullable漂移检查；报告artifact专属结构不冒用页面Result DTO。`go test ./tests/contracts -count=1` 0.192s通过。本工作单元全量Go/新CI结果另续记；不得套用之前绿色CI。
- 需求矩阵保留原PRD release/future分类，但新Goal明确全部77项 `goal_included=true`；校验禁止因原P1/P2/future排除，且进行中/已验证条目必须链接真实代码/测试。本轮回填BASE和REP已实际实现的部分；矩阵其余历史not_started尚需随各工作单元核实更新，不以旧空矩阵否认台账中的已有开发证据。

## 7f之后：报告页面与CI修复

- `7f376a9`的CI `34102058547`：Windows、Ubuntu打包及依赖扫描已成功；镜像失败已定位到新增需求追踪检查找不到web源码，Docker后端测试stage原只复制web的Go文件。现已改为复制完整受dockerignore约束的web源码，仅留在build/test stage；scratch运行镜像仍只有二进制/证书/许可证。新增回归修前FAIL、修后全部contracts0.145s通过。当前环境无Docker命令，实际镜像验证留待新CI，不能仅凭静态测试宣布镜像成功。
- 报告页面接线后全web21文件452tests36.50s、typecheck/lint/build通过；root4文件报告/结果专项75tests3.20s通过。真实浏览器看见detail已ready但列表仍queued，新增按钮状态断言先FAIL，再同步已验证报告状态并明确其他列表行的快照口径，回归通过。
- 实际SQLite应用browser hold200.877s通过（包含手工操作等待）：读取真实TLS来源Run→报告页显式创建新JSON修订2→精确状态ready→JSON26983字节及HTML64405字节下载均通过客户端独立SHA-256/头/长度检查并请求浏览器保存→刷新后旧JSON/HTML修订仍在→机器21/27/C未变→退出。没有验证用户下载目录落盘；不冒称已保存。无console错误/CSP警告，应用临时DB已正常清理。
- M5-06设计草案记录实际运行时闭集、参数硬编码、缺真实校准/盲语料与标签隔离边界；A1工程开始，尚无正式校准或发布准入。两Run比较也仍在开发。

后续实际证据：报告completion权限故障隔离5文件已冻结。修前7种撤权双库错误归一、实际Worker2.512s反例FAIL；修后Report/NonReportPermission/取消/queueGate双库三轮repository60.049s、worker117.541s全部通过。旧文件无发布引用仍不可下载；真实审计INSERT失败、数据库关闭、失租或非报告Permission不得降级，另一真实会话用户的合法报告继续消费，机器结果不变。范围lint0。

两Run比较已完成：全前端23文件491tests、typecheck/lint/build通过。实际app新增同目标Quick18与Custom9的独立估算/确认/TLS执行及不可变旧结果检查，双库三轮35.639s通过。最初测试错误地假定同目标36次请求可在40秒内完成，触发原30RPM限制；改为展示不同包可比性的27次真实请求fixture，未提高限流或放宽业务期限。浏览器hold189.639s通过：真实历史选择左右→固定5GET对比→同修订重读→来源结果→新JSON生成后列表/详情同步ready→退出，无console/CSP错误；189秒包含人工等待，非性能验收。页面显示Quick21/27/C与Custom41/49/C、18/9分母、不同包与Manifest限制，不解释为行为恶化。

CI最终状态补记：`34102058547`失败，Windows/Ubuntu打包和依赖扫描成功；除镜像源码缺失外，quality的repository包race累计600.069s触发Go默认包级10分钟超时，当时ConcurrentMigration/sqlite单项仅1秒，未出现DATA RACE。本地新分片机制正在验证，必须完整枚举Test/Example/Fuzz seed且所有分片必需成功；不能将累计超时误报成产品取消/迁移单项死锁，也不能冒称新CI已绿。

## 外部边界与交付检查

- 独立 GitHub 审批、正式 PO/TL/OPS/SEC/QA 审核、真实付费 API 调用、生产部署及 72 小时观察：待统一审核/待授权执行，绝不虚构。
- 本地 PostgreSQL 已实际执行基础与身份双库测试；Compose/完整产品双库 E2E 仍待完成，不能以局部通过代替。
- RC 仅在功能全链路、双部署、双数据库、算法隔离、安全/性能/故障/恢复证据和 SBOM 齐全后形成。
