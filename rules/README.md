# Versioned rules and probe bundles

Published rules, templates, and tokenizer configurations are immutable and SHA-256 verified. M0-03 only establishes ownership and directory boundaries; M3-M5 add released bundle contents.
